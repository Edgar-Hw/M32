# M32 Task Evidence — 0.0.2-T006

- Task: `0.0.2-T006`
- Version: `0.0.2 Core Adapter`
- Area: basic synchronous host services
- Status: DONE
- Rust: `1.98.0`
- Target: `x86_64-pc-windows-msvc`
- Emulator API schema: `1`

## 적용 내용

M32-owned synchronous host contracts:

- `ClockHost`
- `GuestOutputHost`
- `ExitHost`
- `VibrationHost`

WIE adapter helper:

- `WieBasicHostBridge`

Locked mappings:

```text
Platform::now()               -> ClockHost::epoch_millis()
Platform::write_stdout(...)   -> GuestOutputHost::write_stdout(...)
Platform::write_stderr(...)   -> GuestOutputHost::write_stderr(...)
Platform::exit()              -> ExitHost::request_exit()
Platform::vibrate(...)        -> VibrationHost::vibrate(...)
```

## Emulator API Tests

```text
running 16 tests
...
test tests::clock_host_preserves_full_epoch_millis ... ok
test tests::guest_output_host_preserves_raw_bytes ... ok
test tests::exit_host_represents_guest_exit_request ... ok
test tests::vibration_host_preserves_duration_and_intensity ... ok

test result: ok. 16 passed; 0 failed
```

PASS.

## WIE Adapter Tests

```text
running 15 tests
...
test tests::basic_host_bridge_maps_epoch_millis_to_wie_instant ... ok
test tests::basic_host_bridge_forwards_guest_output_as_raw_bytes ... ok
test tests::basic_host_bridge_forwards_guest_exit_request ... ok
test tests::basic_host_bridge_forwards_vibration_values ... ok

test result: ok. 15 passed; 0 failed
```

PASS.

## Basic Host Verifier

```text
[PASS] ClockHost epoch-millisecond contract
[PASS] raw guest stdout/stderr contract
[PASS] guest exit request contract
[PASS] vibration request contract
[PASS] WIE basic host bridge
[PASS] existing emulator API dependency boundary
Basic host service verification passed.
```

Exit code:

```text
0
```

PASS.

## Dependency Boundary

```text
m32-emulator-api
```

remains free of WIE dependencies.

Adapter depth-1 graph:

```text
m32-wie-adapter
├── m32-emulator-api
├── tracing v0.1.41
├── wie_backend @ f0513eb758c02736981f545ad030eed937d55f3e
└── wie_util @ f0513eb758c02736981f545ad030eed937d55f3e
```

PASS.

## Workspace Tests

Actual tests:

- `m32-desktop`: 9 passed
- `m32-domain`: 3 passed
- `m32-emulator-api`: 16 passed
- `m32-test-fixtures`: 2 passed
- `m32-wie-adapter`: 15 passed

Minimum total:

```text
45 tests passed
```

## Quality Gates

```text
cargo fmt --all -- --check
exit 0

cargo clippy --workspace --all-targets -- -D warnings
exit 0

cargo test --workspace
exit 0

cargo check --workspace --all-targets
exit 0

git diff --check
exit 0
```

PASS.

LF→CRLF messages from `git diff --check` were warnings only; exit code was `0`.

## 판정

`0.0.2-T006 = DONE`

The synchronous WIE host surfaces for time, guest output, guest-exit requests, and vibration are now
represented by M32-owned contracts without exposing WIE types to `m32-emulator-api`.

## Next Exact Task

`0.0.2-T007`

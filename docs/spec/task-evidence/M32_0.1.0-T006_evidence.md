# M32 0.1.0-T006 Evidence — Persistent Storage Wiring

Status before verification: IMPLEMENTED / NOT YET ACCEPTED

## Scope

`PlayableRuntime` opens the existing `PersistentGuestStorage` against the M32 application root supplied by the desktop layer, then passes its database/filesystem hosts into `WiePlatformHosts`. No second storage model is introduced and no SQLite implementation detail moves into `m32-wie-adapter`.

## Automated proof

A composed product-layer test launches the existing M32 RMS fixture, saves through real Java ME `RecordStore`, destroys the runtime, rebuilds the composed runtime against the same root, and requires `M32_RMS_LOADED_OK;`.

# M32 Task Evidence — 0.0.6-T010

- Task: `0.0.6-T010`
- Version: `0.0.6 Storage / Persistence`
- Area: Storage Canonical Integration / Version Close
- Status: DONE
- Rust: `1.98.0`
- Target: `x86_64-pc-windows-msvc`
- WIE revision: `f0513eb758c02736981f545ad030eed937d55f3e`
- RustJava base revision: `ba5797b8eb4cf376fdd63129903d319d1d7acf98`

## Goal

Close the complete `0.0.6 Storage / Persistence` version after T001 through T009.

T010 adds no new guest-visible storage behavior.

## Evidence Set

The final version-close verifier confirmed all prior Storage evidence files:

```text
M32_0.0.6-T001_evidence.md
M32_0.0.6-T002_evidence.md
M32_0.0.6-T003_evidence.md
M32_0.0.6-T004_evidence.md
M32_0.0.6-T005_evidence.md
M32_0.0.6-T006_evidence.md
M32_0.0.6-T007_evidence.md
M32_0.0.6-T008_evidence.md
M32_0.0.6-T009_evidence.md
```

PASS.

## Closed Storage Contract

```text
%LOCALAPPDATA%\M32
├─ storage.sqlite3
└─ guest-files\
```

SQLite:

```text
schema version = 1
journal_mode   = WAL
foreign_keys   = ON
busy_timeout   = 2000ms
rusqlite       = =0.37.0
SQLite         = bundled
```

Persistent RMS/database behavior:

```text
repository open / exists / delete / usage
record next_id / add / get / set / delete / record_ids
record IDs start at 1
record IDs monotonically advance
deleted IDs are not reused
database isolation by exact app_id + database name
```

Persistent filesystem behavior:

```text
exists / size / read / write / truncate
create on write/truncate
write past EOF zero-fills gap
truncate growth zero-fills extension
missing read -> None
EOF read -> Some(0)
AID isolation
parent traversal rejection
safe host component encoding
```

## Real Guest Persistence Proof

The real Java ME fixture uses:

```text
javax.microedition.rms.RecordStore
```

and the final Bundle B chain proved:

```text
Java RecordStore
-> pinned WIE RMS implementation
-> WieDatabaseRepositoryAdapter
-> M32 persistent SQLite host
-> emulator session destroyed
-> storage host destroyed
-> new storage host created against same root
-> new emulator session created
-> same Java MIDlet runs
-> exact persisted 8-byte payload is reloaded
```

Locked guest payload:

```text
M32-RMS1
8 bytes
```

Test:

```text
real_j2me_rms_survives_session_and_storage_rebuild ... ok
```

## WIE Filesystem Restart Proof

The WIE filesystem adapter also passed a platform/storage reconstruction proof using:

```text
AID  = M32 Persistent Filesystem
path = save/state.bin
data = M32-FS-PERSIST
```

After rebuild:

```text
exists == true
size == exact
read == exact persisted bytes
```

Test:

```text
persistent_wie_filesystem_survives_platform_rebuild ... ok
```

## Dependency Boundary

`m32-storage`:

```text
m32-storage -> m32-emulator-api
rusqlite = =0.37.0 with bundled SQLite
```

`m32-wie-adapter`:

```text
m32-storage is dev-only
no production SQLite/storage implementation dependency
```

The final version-close verifier confirmed these boundaries.

## Final Critical Test Baselines

```text
m32-wie-adapter  72 / 72
m32-storage      14 / 14
m32-audio        15 / 15
m32-input         6 / 6
m32-emulator-api 32 / 32
```

Expected minimum workspace test baseline:

```text
153 tests
```

## Final Quality Gates

The final `verify-storage-version-close.ps1` run passed:

```text
complete T001-T009 Storage evidence set
Bundle B canonical functional chain
persistent hosts assemble through WIE platform
deterministic Java ME RecordStore fixture
real guest RMS save -> emulator/storage rebuild -> load
WIE filesystem restart persistence
SQLite WAL / FK / 2000ms / schema-v1 policy
app_id/AID isolation and traversal boundary
exact rusqlite 0.37.0 pin
m32-storage -> m32-emulator-api dependency boundary
m32-wie-adapter -> m32-storage remains dev-only
rustfmt workspace gate
Clippy all-targets -D warnings gate
workspace regression suite
workspace all-target compile gate
git diff whitespace gate
RustJava compatibility-source staged/unstaged boundary
```

Final result:

```text
M32 0.0.6 Storage version-close verification passed.
exit code 0
```

## Repository Scope Immediately Before T010 Evidence

```text
 M Cargo.lock
 M crates/m32-wie-adapter/Cargo.toml
 M crates/m32-wie-adapter/src/lib.rs
?? crates/m32-wie-adapter/test-fixtures/j2me-rms-persistence.jad
?? crates/m32-wie-adapter/test-fixtures/j2me-rms-persistence.jar
?? crates/m32-wie-adapter/test-fixtures/src/m32/RmsPersistenceMidlet.java
?? docs/spec/task-evidence/M32_0.0.6-T006_evidence.md
?? docs/spec/task-evidence/M32_0.0.6-T007_evidence.md
?? docs/spec/task-evidence/M32_0.0.6-T008_evidence.md
?? docs/spec/task-evidence/M32_0.0.6-T009_evidence.md
?? docs/test/STORAGE_BUNDLE_B_WIE_PERSISTENCE.md
?? docs/test/STORAGE_VERSION_CLOSE.md
?? scripts/verify-storage-bundle-b.ps1
?? scripts/verify-storage-version-close.ps1
```

After applying this evidence, add:

```text
?? docs/spec/task-evidence/M32_0.0.6-T010_evidence.md
```

No staged or unstaged RustJava compatibility-source change was observed.

## Non-claims

`0.0.6 Storage / Persistence` does not claim:

```text
complete JSR-75 FileConnection coverage
cloud synchronization
portable save-state snapshots
cross-device save migration
database encryption
per-title storage hacks
```

## Final Verdict

```text
0.0.6-T010 = DONE
0.0.6 Storage / Persistence = 10 / 10 DONE
```

## Overall Progress

```text
0.0.1 Foundation       12 / 12
0.0.2 Core Adapter     12 / 12
0.0.3 First Frame      10 / 10
0.0.4 Input            10 / 10
0.0.5 Audio            10 / 10
0.0.6 Storage          10 / 10

Total DONE             64 / 253
Remaining             189 / 253
```

## Next Version

```text
0.1.0 First Playable
12 tasks
```

# M32 Task Evidence — 0.0.6-T006

- Task: `0.0.6-T006`
- Area: Persistent Hosts -> WIE Platform Assembly
- Version: `0.0.6 Storage / Persistence`
- Bundle: `Bundle B`
- Rust: `1.98.0`
- Target: `x86_64-pc-windows-msvc`
- Status: DONE


## Contract

`m32-storage` provides concrete implementations of the stable M32 host traits:

```text
GuestDatabaseRepositoryHost
GuestDatabaseHost
GuestFilesystemHost
```

These hosts are injected into the existing WIE platform bridge without introducing a direct
production dependency from `m32-wie-adapter` to SQLite internals.

Dependency boundary:

```text
m32-storage -> m32-emulator-api

m32-wie-adapter
  production: m32-emulator-api / WIE
  dev-only:   m32-storage
```

Test:

```text
persistent_storage_hosts_are_accepted_by_wie_platform ... ok
```

Verifier:

```text
[PASS] T006 concrete persistent RMS/filesystem hosts assemble through WIE Platform
[PASS] T006 m32-storage remains dev-only in m32-wie-adapter
```

```text
0.0.6-T006 = DONE
```

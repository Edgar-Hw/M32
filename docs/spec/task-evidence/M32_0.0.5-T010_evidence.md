# M32 Task Evidence — 0.0.5-T010

- Task: `0.0.5-T010`
- Version: `0.0.5 Audio`
- Area: Audio Canonical Integration / Version Close
- Status: DONE
- Rust: `1.98.0`
- Target: `x86_64-pc-windows-msvc`
- WIE revision: `f0513eb758c02736981f545ad030eed937d55f3e`
- RustJava base revision: `ba5797b8eb4cf376fdd63129903d319d1d7acf98`

## Goal

Close the complete `0.0.5 Audio` version after T001 through T009.

T010 adds no new guest-visible runtime feature.

## Evidence Gate

The version-close verifier confirmed the complete T001-T009 Audio evidence set:

```text
M32_0.0.5-T001_evidence.md
M32_0.0.5-T002_evidence.md
M32_0.0.5-T003_evidence.md
M32_0.0.5-T004_evidence.md
M32_0.0.5-T005_evidence.md
M32_0.0.5-T006_evidence.md
M32_0.0.5-T007_evidence.md
M32_0.0.5-T008_evidence.md
M32_0.0.5-T009_evidence.md
```

PASS.

T009 evidence also contains:

```text
MANUAL_AUDIBLE_SMOKE: PASS
```

PASS.

## Closed Audio Contract

```text
48000 Hz canonical output
f32 internal samples
2-channel stereo
i16 mono/stereo normalization
deterministic linear resampling
deterministic stereo summing/saturation
thread-safe GuestAudioHost ingress
real J2ME MMAPI/SMAF transport
real WIE -> M32 audio bridge
exact timed Wave scheduling
u32 audio-handle voice identity
Stop lifecycle
deterministic repeat
baseline MIDI NoteOn/NoteOff rendering
Windows CPAL output
60ms latency target
80ms / 3840-frame pause fade
physical audible Windows smoke
```

## Real Java Guest Proof

The real pinned runtime path remains proven:

```text
m32.AudioMidlet
-> javax.microedition.media.Manager.createPlayer
-> pinned WIE SmafPlayer
-> Player.start / Player.stop
-> WIE Audio
-> WieAudioSinkAdapter
-> M32 GuestAudioHost
```

Tests:

```text
audio_mmapi_fixture_locks_guest_manager_player_contract ... ok
real_j2me_mmapi_start_stop_reaches_m32_audio_host ... ok
```

## Runtime Audio Proof

`m32-audio` final baseline:

```text
15 passed
0 failed
```

Bundle B deterministic runtime tests passed:

```text
realtime_host_schedules_wave_at_exact_millisecond_frame
stop_by_handle_removes_active_voice_before_next_render
repeat_restarts_prepared_wave_deterministically
baseline_midi_note_on_off_renders_signal_then_stops
pause_fade_reaches_zero_in_exact_3840_frames_and_freezes_voice
```

## Windows Physical Output Proof

The prior T009 physical smoke run observed:

```text
device: FxSound Speakers(FxSound Audio Enhancer)
sample rate: 48000 Hz
channels: 2
callback buffer: device default (2880-frame request unsupported/unknown)
Playing a 440Hz M32 test tone for 1 second...
M32 audio smoke finished.
exit code 0
```

The user explicitly confirmed the 440Hz test tone was audibly heard.

The 60ms / 2880-frame value remains a target. When the device does not expose that exact callback
buffer request, the device default is used rather than falsely claiming fixed hardware latency.

## Dependency Boundary

Unconditional/core production dependency:

```text
m32-audio -> m32-emulator-api
```

Windows target-specific production dependency:

```text
cpal = =0.18.2
```

The version-close verifier confirmed:

```text
exactly one unconditional m32-audio core dependency
m32-emulator-api is workspace-local/path-based
CPAL dependency exists exactly once
CPAL is pinned exactly to =0.18.2
CPAL remains Windows target-specific
```

No direct WIE dependency is added to `m32-audio`.

## Final Regression Baseline

Observed critical package baselines:

```text
m32-audio          15 / 15
m32-emulator-api   32 / 32
m32-input           6 / 6
m32-wie-adapter    68 / 68
```

Expected minimum workspace regression count:

```text
135 tests
```

Zero-test workspace crates are additional members and do not reduce this minimum.

## Final Close Gates

The final version-close run passed:

```text
complete T001-T009 Audio evidence set
T009 physical audible Windows smoke evidence
Bundle B canonical functional chain
real Java MMAPI -> WIE -> M32 audio path
48kHz f32 stereo canonical pipeline
deterministic Wave scheduling / Stop / repeat
baseline MIDI NoteOn/NoteOff renderer
exact 80ms / 3840-frame pause fade
Windows CPAL output boundary
exact CPAL 0.18.2 pin
m32-audio unconditional core dependency boundary
rustfmt workspace gate
Clippy all-targets -D warnings gate
workspace regression suite
workspace all-target compile gate
git diff whitespace gate
RustJava compatibility-source staged/unstaged boundary
```

Final result:

```text
M32 0.0.5 Audio version-close verification passed.
exit code 0
```

## Repository Scope at Close

```text
 M Cargo.lock
 M crates/m32-audio/Cargo.toml
 M crates/m32-audio/src/lib.rs
 M crates/m32-wie-adapter/src/lib.rs
 M scripts/verify-wie-audio-bundle-a.ps1
?? crates/m32-audio/examples/
?? crates/m32-wie-adapter/test-fixtures/j2me-audio-mmapi.jad
?? crates/m32-wie-adapter/test-fixtures/j2me-audio-mmapi.jar
?? crates/m32-wie-adapter/test-fixtures/src/m32/AudioMidlet.java
?? docs/spec/task-evidence/M32_0.0.5-T006_evidence.md
?? docs/spec/task-evidence/M32_0.0.5-T007_evidence.md
?? docs/spec/task-evidence/M32_0.0.5-T008_evidence.md
?? docs/spec/task-evidence/M32_0.0.5-T009_evidence.md
?? docs/test/WIE_AUDIO_BUNDLE_B_FIX2_CPAL018_API.md
?? docs/test/WIE_AUDIO_BUNDLE_B_FIX3_PAUSE_FADE_BOUNDARY.md
?? docs/test/WIE_AUDIO_BUNDLE_B_FIX4_JAR_MAGIC_ASSERTION.md
?? docs/test/WIE_AUDIO_BUNDLE_B_FIX5_BUNDLE_A_DEPENDENCY_VERIFIER.md
?? docs/test/WIE_AUDIO_BUNDLE_B_RUNTIME_WINDOWS.md
?? docs/test/WIE_AUDIO_VERSION_CLOSE.md
?? scripts/verify-wie-audio-bundle-b.ps1
?? scripts/verify-wie-audio-version-close.ps1
```

No staged or unstaged RustJava compatibility-source change was observed.

## Non-claims

`0.0.5 Audio` does not claim:

```text
full General MIDI instrument fidelity
SoundFont-quality synthesis
complete SMAF/vendor timbre fidelity
all J2ME media MIME types
per-game audio compatibility profiles
fixed 60ms latency on every Windows device
```

These remain later compatibility/product-quality scope.

## Final 판정

```text
0.0.5-T010 = DONE
0.0.5 Audio = 10 / 10 DONE
```

## Overall Progress

```text
0.0.1 Foundation     12 / 12
0.0.2 Core Adapter   12 / 12
0.0.3 First Frame    10 / 10
0.0.4 Input          10 / 10
0.0.5 Audio          10 / 10

Total DONE           54 / 253
Remaining            199 / 253
```

## Next Version

```text
0.0.6 Storage / Persistence
10 tasks
```

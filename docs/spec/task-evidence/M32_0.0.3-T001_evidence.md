# M32 Task Evidence — 0.0.3-T001

- Task: `0.0.3-T001`
- Version: `0.0.3 First Frame`
- Area: Explicit JAD + JAR Launch Path
- Status: DONE
- Rust: `1.98.0`
- Target: `x86_64-pc-windows-msvc`
- WIE revision: `f0513eb758c02736981f545ad030eed937d55f3e`

## 적용 내용

- `create_j2me_jad_jar_session(...)` 추가
- pinned `J2MEEmulator::from_jad_jar(...)` 직접 연결
- M32 `WiePlatformHosts` -> `WiePlatformAdapter` -> boxed WIE Platform ownership 전달
- JAD bytes / JAR filename / JAR bytes 전달
- concrete J2ME emulator를 기존 `WieSession`으로 wrapping
- M32 session 반환 상태를 `Ready`로 유지
- WIE concrete type이 `m32-emulator-api` 경계를 넘지 않음
- 신규 dependency 없음
- Cargo.lock 변경 없음

## Targeted Tests

```text
j2me_jad_jar_factory_constructs_ready_m32_session ... ok
j2me_jad_jar_factory_uses_explicit_launch_descriptor_path ... ok
```

각 targeted test:

```text
1 passed
0 failed
exit 0
```

## Adapter Tests

```text
running 42 tests

test result: ok. 42 passed; 0 failed
```

PASS.

## Emulator API Tests

```text
running 29 tests

test result: ok. 29 passed; 0 failed
```

PASS.

## First Frame JAD+JAR Launch Verifier

```text
[PASS] pinned J2ME from_jad_jar constructor path
[PASS] explicit MIDlet-1 launch descriptor
[PASS] JAD bytes and JAR ownership transfer
[PASS] Ready M32 session result
[PASS] no new dependency
[PASS] previous Core Adapter verification chain
First Frame JAD+JAR launch verification passed.
```

Exit code:

```text
0
```

PASS.

The verifier also reran the complete Core Adapter verification chain successfully.

## Workspace Tests

Actual M32 tests:

- `m32-desktop`: 9
- `m32-domain`: 3
- `m32-emulator-api`: 29
- `m32-test-fixtures`: 2
- `m32-wie-adapter`: 42

Minimum total:

```text
85 tests passed
```

## Quality Gates

```text
cargo fmt --all -- --check
exit 0

cargo check -p m32-wie-adapter
exit 0

cargo clippy --workspace --all-targets -- -D warnings
exit 0

cargo test --workspace
exit 0

cargo check --workspace --all-targets
exit 0

git diff --check
exit 0
```

PASS.

LF->CRLF warning was non-blocking; `git diff --check` returned `0`.

## 판정

```text
0.0.3-T001 = DONE
```

## Next Exact Task

```text
0.0.3-T002
Deterministic bootable MIDlet fixture
```

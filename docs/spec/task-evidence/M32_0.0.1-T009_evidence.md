# M32 Task Evidence — 0.0.1-T009

- Task: `0.0.1-T009`
- Area: logging
- Status: DONE
- Rust: `1.98.0`
- Target: `x86_64-pc-windows-msvc`

## 적용 내용

- `tracing = 0.1.44` exact workspace dependency 추가
- `tracing-subscriber = 0.3.23` exact workspace dependency 추가
- `m32-desktop` structured logging 초기화 추가
- `M32_LOG` level override 추가
- 기본 INFO level 및 invalid-value fallback 구현
- startup `app_start` structured event 추가
- logging privacy/safety policy 추가
- Third-Party Notices 갱신

## 실제 Dependency 검증

`cargo check --workspace`에서 lockfile에 다음 direct dependency가 추가됨:

```text
tracing v0.1.44
tracing-subscriber v0.3.23
```

`cargo tree -p m32-desktop`에서도 동일 버전 확인.

PASS.

## Logging Unit Tests

```text
running 3 tests
test logging::tests::accepted_levels_are_case_insensitive ... ok
test logging::tests::invalid_level_falls_back_to_info ... ok
test logging::tests::default_level_is_info ... ok

test result: ok. 3 passed; 0 failed
```

PASS.

## Startup Structured Event

실제 실행에서 다음 구조화 필드 확인:

- `event="app_start"`
- `app_version`
- `product_spec_version`
- `spec_bundle_version`
- `git_commit`
- `wie_commit`
- `rust_version`
- `build_target`
- `build_profile`

모든 `Select-String` 검증 결과 `True`.

PASS.

## Invalid M32_LOG Fallback

`M32_LOG=banana` 환경에서:

- `falling back to INFO` 검색 결과 `True`
- `M32 application started` 검색 결과 `True`

잘못된 설정이 application startup을 중단하지 않음.

PASS.

## Quality Gates

```text
cargo fmt --all -- --check
exit 0

cargo clippy --workspace --all-targets -- -D warnings
exit 0

cargo test --workspace
exit 0

cargo check --workspace
exit 0

git diff --check
exit 0
```

PASS.

전체 workspace의 실제 unit tests:

- `m32-desktop` logging: 3 passed
- `m32-domain` BuildInfo: 3 passed
- `m32-test-fixtures`: 2 passed

최소 8 tests passed.

Windows working tree의 LF→CRLF 예정 경고가 일부 출력되었으나
`git diff --check` exit code는 `0`이며 whitespace 오류는 아님.

## 판정

`0.0.1-T009 = DONE`

M32 application이 BuildInfo를 포함한 구조화된 local diagnostics event를 출력할 수 있고,
logging level fallback 및 dependency baseline이 검증되었다.

## Next Exact Task

`0.0.1-T010`

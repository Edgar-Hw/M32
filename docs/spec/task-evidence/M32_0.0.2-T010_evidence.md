# M32 Task Evidence — 0.0.2-T010

- Task: `0.0.2-T010`
- Version: `0.0.2 Core Adapter`
- Area: WIE Platform Assembly
- Status: DONE
- Rust: `1.98.0`
- Target: `x86_64-pc-windows-msvc`
- Emulator API schema: `1`
- WIE revision: `f0513eb758c02736981f545ad030eed937d55f3e`

## 적용 내용

- `WiePlatformHosts` 추가
- `WiePlatformAdapter` 추가
- pinned `wie_backend::Platform` 완전 구현
- Display / Clock / Database / Filesystem / Audio / Stdout / Stderr / Exit / Vibration 조립
- `audio_sink()` 호출마다 fresh `WieAudioSinkAdapter` 생성
- 모든 WIE AudioSink가 동일 `Arc<dyn GuestAudioHost>` 공유
- Platform clock -> `Instant::from_epoch_millis(...)` 변환
- guest exit은 process termination이 아니라 M32 `ExitHost::request_exit()`로 유지
- 신규 dependency 없음
- M32/WIE dependency boundary 유지

## FIX1

초기 T010 production library:

```text
cargo check -p m32-wie-adapter
exit 0
```

으로 정상 컴파일됨.

그러나 `#[cfg(test)]` helper를 large tuple에서 `RecordingPlatformFixture`로 변환하는 package
후처리 중 변수명 치환이 test boundary를 넘어가면서 8개의 `E0425`가 발생함.

FIX1은 test-only binding을 정확히 복구함.

Existing BasicHostBridge tests:

```text
fixture.output     -> output
fixture.exit       -> exit
fixture.vibration  -> vibration
```

New Platform test:

```text
output             -> fixture.output
exit               -> fixture.exit
vibration          -> fixture.vibration
```

Production Platform behavior, public API, dependency, WIE revision은 변경하지 않음.

## Final Adapter Tests

```text
running 35 tests
...
test tests::wie_platform_adapter_implements_pinned_platform_contract ... ok
test tests::wie_platform_creates_audio_sink_backed_by_shared_m32_audio_host ... ok
test tests::wie_platform_delegates_output_exit_and_vibration ... ok
test tests::wie_platform_delegates_screen_clock_database_and_filesystem ... ok

test result: ok. 35 passed; 0 failed
```

PASS.

## Platform Assembly Verifier

```text
[PASS] WiePlatformAdapter pinned Platform implementation
[PASS] display and clock assembly
[PASS] database and filesystem assembly
[PASS] stdout/stderr, exit, and vibration assembly
[PASS] shared M32 audio host across fresh WIE AudioSink objects
[PASS] existing emulator API dependency boundary
WIE platform assembly verification passed.
```

Exit code:

```text
0
```

PASS.

The verifier also reran the T009 Audio Bridge verifier successfully.

## Workspace Tests

Actual tests:

- `m32-desktop`: 9
- `m32-domain`: 3
- `m32-emulator-api`: 27
- `m32-test-fixtures`: 2
- `m32-wie-adapter`: 35

Minimum total:

```text
76 tests passed
```

## Quality Gates

```text
cargo fmt --all -- --check
exit 0

cargo test -p m32-wie-adapter
35 passed
exit 0

cargo clippy --workspace --all-targets -- -D warnings
exit 0

cargo test --workspace
exit 0

cargo check --workspace --all-targets
exit 0

git diff --check
exit 0
```

PASS.

LF→CRLF messages from `git diff --check` were warnings only.

## 판정

`0.0.2-T010 = DONE`

All nine pinned WIE host surfaces are now assembled into one concrete `WiePlatformAdapter` ready
to be boxed and transferred into a WIE emulator constructor.

## Next Exact Task

`0.0.2-T011`

# M32 Task Evidence — 0.0.5-T007

- Task: `0.0.5-T007`
- Version: `0.0.5 Audio`
- Area: Real Guest -> WIE -> M32 Audio Integration
- Status: DONE

## Real Guest Path

The deterministic Java fixture executes through the real pinned runtime:

```text
AudioMidlet.startApp
-> javax.microedition.media.Manager.createPlayer
-> pinned WIE SmafPlayer
-> Player.start
-> WIE Audio::play
-> WieAudioSinkAdapter
-> M32 GuestAudioHost

Player.stop
-> WIE Audio::stop
-> WieAudioSinkAdapter
-> M32 GuestAudioHost
```

Observed canonical host commands:

```text
Play { handle=0, duration=0, events=[], repeat=false }
Stop { handle=0 }
```

Integration test:

```text
real_j2me_mmapi_start_stop_reaches_m32_audio_host ... ok
```

The session reached Running and both guest sentinels were observed.

## Regression

`m32-wie-adapter` final Bundle B baseline:

```text
68 passed
0 failed
```

## Result

```text
0.0.5-T007 = DONE
```

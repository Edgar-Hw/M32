# M32 Task Evidence — 0.0.2-T009

- Task: `0.0.2-T009`
- Version: `0.0.2 Core Adapter`
- Area: Guest Audio Host Contract / WIE AudioSink Bridge
- Status: DONE
- Rust: `1.98.0`
- Target: `x86_64-pc-windows-msvc`
- Emulator API schema: `1`
- WIE revision: `f0513eb758c02736981f545ad030eed937d55f3e`

## 적용 내용

- `GuestAudioHandle = u32` 추가
- M32-owned `GuestAudioCommand` 추가
- M32-owned `GuestAudioSequence` 추가
- M32-owned `GuestTimedAudioEvent` 추가
- M32-owned `GuestAudioEventData` 추가
- `GuestAudioHost` / `GuestAudioHostError` 추가
- `WieAudioSinkAdapter` 구현
- WIE Play/Stop command -> M32 command mapping 구현
- MIDI raw byte stream 보존
- Wave channels/sampling_rate/i16 sample 보존
- sequence duration/event time/repeat 값 보존
- WIE `Arc<AudioSequence>` ownership을 adapter 밖으로 노출하지 않고 M32-owned data로 복사
- audio dispatch failure를 non-panicking structured warning으로 처리
- warning에서 MIDI bytes/PCM samples/free-form error message 비노출
- `m32-emulator-api` WIE dependency-free boundary 유지
- 신규 dependency 없음

## Emulator API Tests

```text
running 27 tests
...
test tests::guest_audio_handle_is_exact_u32 ... ok
test tests::guest_audio_host_error_keeps_stable_code_and_message ... ok
test tests::guest_audio_midi_event_preserves_raw_bytes ... ok
test tests::guest_audio_play_and_stop_preserve_handle_repeat_and_sequence ... ok
test tests::guest_audio_wave_event_preserves_format_and_i16_samples ... ok

test result: ok. 27 passed; 0 failed
```

PASS.

## WIE Adapter Tests

```text
running 31 tests
...
test tests::wie_audio_host_failure_is_non_panicking ... ok
test tests::wie_audio_play_maps_handle_repeat_duration_and_midi_bytes ... ok
test tests::wie_audio_sink_adapter_implements_pinned_audio_sink_contract ... ok
test tests::wie_audio_stop_maps_exact_handle ... ok
test tests::wie_audio_wave_maps_channels_sampling_rate_and_i16_samples ... ok

test result: ok. 31 passed; 0 failed
```

PASS.

## Audio Bridge Verifier

```text
[PASS] M32 GuestAudioHost contract
[PASS] exact u32 audio handle contract
[PASS] MIDI byte-preserving bridge
[PASS] Wave format/sample-preserving bridge
[PASS] Play/Stop command mapping
[PASS] non-panicking audio host failure handling
[PASS] existing emulator API dependency boundary
WIE audio bridge verification passed.
```

Exit code:

```text
0
```

PASS.

## Dependency Boundary

`m32-emulator-api`:

```text
m32-emulator-api v0.0.0
```

No WIE dependency.

Adapter depth-1 graph:

```text
m32-wie-adapter
├── async-trait v0.1.92
├── m32-emulator-api
├── tracing v0.1.41
├── wie_backend @ f0513eb758c02736981f545ad030eed937d55f3e
└── wie_util @ f0513eb758c02736981f545ad030eed937d55f3e
```

No new audio dependency.

PASS.

## Workspace Tests

Actual tests:

- `m32-desktop`: 9
- `m32-domain`: 3
- `m32-emulator-api`: 27
- `m32-test-fixtures`: 2
- `m32-wie-adapter`: 31

Minimum total:

```text
72 tests passed
```

## Quality Gates

```text
cargo fmt --all -- --check
exit 0

cargo check -p m32-wie-adapter
exit 0

cargo clippy --workspace --all-targets -- -D warnings
exit 0

cargo test --workspace
exit 0

cargo check --workspace --all-targets
exit 0

git diff --check
exit 0
```

PASS.

LF→CRLF messages from `git diff --check` were warnings only; exit code was `0`.

## 판정

`0.0.2-T009 = DONE`

All nine pinned WIE host-service categories now have M32-owned boundaries or bridges:
Display, Clock, Database, Filesystem, Audio, Stdout, Stderr, Exit, and Vibration.

## Next Exact Task

`0.0.2-T010`

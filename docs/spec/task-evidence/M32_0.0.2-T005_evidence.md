# M32 Task Evidence — 0.0.2-T005

- Task: `0.0.2-T005`
- Version: `0.0.2 Core Adapter`
- Area: Display Host Contract / WIE Screen Bridge
- Status: DONE
- Rust: `1.98.0`
- Target: `x86_64-pc-windows-msvc`
- Emulator API schema: `1`

## 적용 내용

- M32-owned `DisplaySize` 추가
- M32-owned canonical `RgbaFrame` 추가
- RGBA8 byte-length validation 추가
- `DisplayHost` contract 추가
- display host error code/message contract 추가
- `WieScreenAdapter` 구현
- pinned WIE `Screen` trait 구현
- WIE resize/redraw -> M32 DisplayHost delegation 구현
- WIE Image -> canonical RGBA8 reference conversion 구현
- paint/present failure structured logging 및 frame-drop semantics 적용
- direct pinned `wie_util` adapter dependency 추가
- WIE display types를 `m32-emulator-api`에 노출하지 않는 boundary 유지

Canonical frame:

```text
RGBA8
R,G,B,A
top-to-bottom rows
left-to-right pixels
byte length = width * height * 4
```

## 적용 확인 이슈

초기 T005 검증에서는 저장소에 T005 ZIP이 실제로 덮어써지지 않아 기존 source가 실행됨.

관찰값:

```text
m32-emulator-api: 6 tests
m32-wie-adapter: 5 tests
verify-wie-display-bridge.ps1: file missing
```

따라서 해당 실행은 T005 코드 검증으로 인정하지 않음.

재적용 package의 `verify-t005-applied.ps1`로 다음을 확인함:

```text
[PASS] DisplayHost contract
[PASS] RgbaFrame contract
[PASS] DisplaySize contract
[PASS] WieScreenAdapter
[PASS] adapter tracing dependency
[PASS] adapter wie_util dependency
[PASS] workspace wie_util pin
[PASS] display verifier
[PASS] m32-emulator-api source test count: 12
[PASS] m32-wie-adapter source test count: 11
T005 source application verification passed.
```

Exit code `0`.

## FIX1 — PowerShell verifier array matching

실제 T005 code/dependency는 정상 상태였으나 첫 display verifier가 `wie_util` direct dependency를
잘못 실패 판정함.

실제 Cargo tree:

```text
m32-wie-adapter
├── m32-emulator-api
├── tracing v0.1.41
├── wie_backend @ f0513eb758c02736981f545ad030eed937d55f3e
└── wie_util @ f0513eb758c02736981f545ad030eed937d55f3e
```

원인:

```powershell
if ($tree -notmatch "wie_util")
```

에서 `$tree`가 다중 line array라 `-notmatch` 결과가 비어 있지 않아 false failure 발생.

FIX1:

```powershell
$wieUtilMatch = $tree | Select-String -SimpleMatch "wie_util"
if ($null -eq $wieUtilMatch) {
    throw ...
}
```

Rust/Cargo source는 변경하지 않고 verifier만 수정함.

## 최종 Emulator API Tests

```text
running 12 tests
...
test tests::display_size_calculates_rgba8_length ... ok
test tests::rgba_frame_accepts_exact_byte_length ... ok
test tests::rgba_frame_rejects_byte_length_mismatch ... ok
test tests::display_host_error_keeps_stable_code_and_message ... ok

test result: ok. 12 passed; 0 failed
```

PASS.

## 최종 WIE Adapter Tests

```text
running 11 tests
...
test tests::wie_screen_adapter_implements_pinned_screen_contract ... ok
test tests::wie_screen_resize_and_redraw_delegate_to_m32_host ... ok
test tests::wie_screen_paint_converts_colors_to_canonical_rgba8 ... ok
test tests::display_host_error_maps_to_wie_fatal_error ... ok

test result: ok. 11 passed; 0 failed
```

PASS.

## 최종 Display Bridge Verifier

```text
[PASS] M32 DisplayHost contract
[PASS] canonical RGBA8 frame validation
[PASS] WieScreenAdapter compile bridge
[PASS] synthetic WIE image -> M32 RGBA8 conversion
[PASS] direct pinned wie_util adapter dependency
[PASS] existing emulator API dependency boundary
WIE display bridge verification passed.
```

Exit code:

```text
0
```

PASS.

## Workspace Quality

이전 full T005 run:

```text
cargo clippy --workspace --all-targets -- -D warnings
exit 0

cargo test --workspace
exit 0

cargo check --workspace --all-targets
exit 0
```

최종 FIX1 이후:

```text
cargo fmt --all -- --check
exit 0

verify-wie-display-bridge.ps1
exit 0

git diff --check
exit 0
```

PASS.

최종 실제 unit tests:

- `m32-desktop`: 9
- `m32-domain`: 3
- `m32-emulator-api`: 12
- `m32-test-fixtures`: 2
- `m32-wie-adapter`: 11

최소 총:

```text
37 tests passed
```

## 판정

`0.0.2-T005 = DONE`

WIE `Screen` now has a validated bridge into an M32-owned canonical RGBA8 display boundary without
introducing desktop/window/GPU concerns into the adapter.

## Next Exact Task

`0.0.2-T006`

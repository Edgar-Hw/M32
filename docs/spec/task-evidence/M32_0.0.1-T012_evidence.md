# M32 Task Evidence — 0.0.1-T012

- Task: `0.0.1-T012`
- Version: `0.0.1 Foundation Lock`
- Area: benchmark
- Status: DONE
- Rust: `1.98.0`
- Target: `x86_64-pc-windows-msvc`
- Benchmark schema: `1`

## 적용 내용

- Foundation benchmark Cargo profile 고정
- 240×320 RGBA framebuffer copy workload 추가
- 240×320 RGB565 → RGBA8 conversion workload 추가
- 240×320 → 720×960 integer 3× scale workload 추가
- canonical PowerShell benchmark runner 추가
- benchmark output schema v1 고정
- 절대 시간 threshold를 Foundation hard gate로 사용하지 않는 정책 확정

## FIX1

초기 all-target Clippy 검증에서 Rust 1.98.0의
`clippy::chunks-exact-to-as-chunks`가 다음 코드를 거부함.

```text
destination.chunks_exact_mut(4)
```

`#[allow(...)]`로 억제하지 않고 Clippy 권장 API인:

```text
destination.as_chunks_mut::<4>()
```

형태로 수정함.

FIX1 이후:

```text
cargo clippy --workspace --all-targets -- -D warnings
exit 0
```

PASS.

## 실제 Baseline Run

최종 FIX1 적용 후 실제 출력:

```text
M32_BASELINE_BENCHMARK schema_version=1
logical_width=240
logical_height=320
M32_BENCH name=rgba_copy_240x320 iterations=4000 total_ns=34060000 ns_per_iteration=8515 checksum=509808
M32_BENCH name=rgb565_to_rgba_240x320 iterations=1000 total_ns=88996000 ns_per_iteration=88996 checksum=3782895024950539189
M32_BENCH name=integer_scale_3x_240x320 iterations=80 total_ns=16379100 ns_per_iteration=204738 checksum=342952756600
M32_BASELINE_BENCHMARK result=PASS
```

Benchmark process exit code:

```text
0
```

결과 line count:

```text
3
```

모든 schema/size/workload/result 검색 결과 `True`.

측정 시간은 machine-specific evidence이며 LOCK 값이 아니다.

## Quality Gates

```text
cargo fmt --all -- --check
exit 0

cargo clippy --workspace --all-targets -- -D warnings
exit 0

cargo test --workspace
exit 0

cargo check --workspace --all-targets
exit 0

git diff --check
exit 0
```

PASS.

기존 실제 unit tests도 유지됨:

- `m32-desktop`: 9 passed
- `m32-domain`: 3 passed
- `m32-test-fixtures`: 2 passed

최소 14 tests passed.

## 판정

`0.0.1-T012 = DONE`

`0.0.1 Foundation Lock = 12 / 12 DONE`

Foundation 단계의 workspace/toolchain/quality/CI/legal/governance/fixture/build-info/logging/
crash/runtime-path/benchmark 기준선이 모두 구축되고 검증되었다.

## Next Version

`0.0.2 Core Adapter`

## Next Exact Task

`0.0.2-T001`

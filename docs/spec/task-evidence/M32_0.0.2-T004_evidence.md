# M32 Task Evidence — 0.0.2-T004

- Task: `0.0.2-T004`
- Version: `0.0.2 Core Adapter`
- Area: WIE host service inventory
- Status: DONE
- Rust: `1.98.0`
- Target: `x86_64-pc-windows-msvc`
- Emulator API schema: `1`

## 적용 내용

- `HostServiceKind` M32-owned enum 추가
- stable host-service ID 9종 고정
- `EmulatorBackend::required_host_services()` 계약 추가
- `WieBackendAdapter`가 pinned WIE Platform 요구사항 9종을 선언
- pinned WIE `Platform` surface 전체 compile probe 추가
- WIE Platform/Screen/Filesystem/Database/Audio 타입을 M32 public API에 노출하지 않는 경계 유지

Locked WIE host service mapping:

```text
screen()              -> display
now()                 -> clock
database_repository() -> database
filesystem()          -> filesystem
audio_sink()          -> audio
write_stdout()        -> stdout
write_stderr()        -> stderr
exit()                -> exit
vibrate()             -> vibration
```

## Emulator API Unit Tests

```text
running 8 tests
...
test tests::host_service_ids_are_stable ... ok
test tests::host_service_kinds_are_distinct ... ok

test result: ok. 8 passed; 0 failed
```

PASS.

## WIE Adapter Unit Tests

```text
running 7 tests
...
test tests::adapter_declares_all_wie_platform_host_requirements ... ok
test tests::pinned_wie_platform_surface_is_still_compatible ... ok

test result: ok. 7 passed; 0 failed
```

PASS.

## Host Service Inventory Verification

Canonical verifier output confirmed:

```text
[PASS] m32-emulator-api has no WIE dependency
[PASS] m32-wie-adapter -> m32-emulator-api
[PASS] m32-wie-adapter -> wie_backend
[PASS] m32-desktop has no direct WIE dependency
[PASS] M32 HostServiceKind contract
[PASS] WIE host requirement inventory
[PASS] pinned WIE Platform compile probe
[PASS] existing emulator API dependency boundary
WIE host service inventory verification passed.
```

Exit code:

```text
0
```

PASS.

## Cargo Boundary

`m32-emulator-api` remains dependency-free.

Adapter direct dependency shape remains:

```text
m32-wie-adapter
├── m32-emulator-api
└── wie_backend @ f0513eb758c02736981f545ad030eed937d55f3e
```

PASS.

## Workspace Tests

Actual tests:

- `m32-desktop`: 9 passed
- `m32-domain`: 3 passed
- `m32-emulator-api`: 8 passed
- `m32-test-fixtures`: 2 passed
- `m32-wie-adapter`: 7 passed

Minimum total:

```text
29 tests passed
```

## Quality Gates

```text
cargo fmt --all -- --check
exit 0

cargo clippy --workspace --all-targets -- -D warnings
exit 0

cargo test --workspace
exit 0

cargo check --workspace --all-targets
exit 0

git diff --check
exit 0
```

PASS.

Windows LF→CRLF notices from `git diff --check` were warnings only; exit code was `0`.

## 판정

`0.0.2-T004 = DONE`

M32 now has a stable, implementation-independent inventory of every host service required by the
pinned WIE Platform surface, with compile-time drift detection.

## Next Exact Task

`0.0.2-T005`

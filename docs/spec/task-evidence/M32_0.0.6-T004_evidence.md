# M32 Task Evidence — 0.0.6-T004

- Task: `0.0.6-T004`
- Area: Per-game Isolation / Traversal Boundary
- Version: `0.0.6 Storage / Persistence`
- Bundle: `Bundle A`
- Rust: `1.98.0`
- Target: `x86_64-pc-windows-msvc`
- Status: DONE


## Contract

Database storage is isolated by exact `app_id`.

Guest filesystem storage is isolated by AID.

AID and guest path components are encoded into safe host path components rather than concatenated
as raw Windows filesystem paths.

Rejected path conditions include:

```text
.
..
NUL
path with no file component
```

This prevents parent traversal and host path escape.

Tests:

```text
filesystem_rejects_parent_traversal_and_empty_file_path ... ok
filesystem_aids_are_isolated_logically ... ok
combined_storage_reopen_preserves_database_and_file_without_cross_game_leak ... ok
```

Verifier:

```text
[PASS] T004 app_id/AID isolation boundary
[PASS] T004 parent-traversal rejection
```

No title/hash-specific branch was introduced.

```text
0.0.6-T004 = DONE
```

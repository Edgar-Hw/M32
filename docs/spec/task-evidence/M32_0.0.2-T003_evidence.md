# M32 Task Evidence — 0.0.2-T003

- Task: `0.0.2-T003`
- Version: `0.0.2 Core Adapter`
- Area: emulator session contract
- Status: DONE
- Rust: `1.98.0`
- Target: `x86_64-pc-windows-msvc`
- Emulator API schema: `1`

## 적용 내용

- `SessionState` M32-owned lifecycle contract 추가
- `Ready`, `Running`, `Faulted` 상태 정의
- `SessionErrorCode::BackendTickFailed` 추가
- `EmulatorSessionError` 추가
- `EmulatorSession` trait 추가
- WIE concrete emulator를 감싸는 `WieSession` 추가
- WIE `tick()` 성공 시 M32 session state를 `Running`으로 전이
- WIE `tick()` 실패 시 M32 session state를 `Faulted`로 전이
- WIE tick error를 stable M32 `BackendTickFailed` code로 변환
- WIE error type을 `m32-emulator-api`에 노출하지 않는 경계 유지
- 입력/Event/KeyCode translation은 후속 Task로 분리

## Emulator API Unit Tests

```text
running 6 tests
test tests::api_schema_is_version_one ... ok
test tests::backend_descriptor_keeps_identity_fields ... ok
test tests::backend_trait_is_implementation_agnostic ... ok
test tests::session_state_contract_is_explicit ... ok
test tests::session_trait_is_implementation_agnostic ... ok
test tests::session_error_keeps_stable_code_and_message ... ok

test result: ok. 6 passed; 0 failed
```

PASS.

## WIE Adapter Unit Tests

```text
running 5 tests
test tests::adapter_exposes_wie_identity_through_m32_api ... ok
test tests::wie_session_implements_m32_session_contract ... ok
test tests::wie_tick_error_maps_to_stable_m32_error_code ... ok
test tests::upstream_revision_matches_locked_baseline ... ok
test tests::pinned_wie_backend_options_api_is_available ... ok

test result: ok. 5 passed; 0 failed
```

PASS.

## Session Contract Verification

Canonical verifier confirmed:

```text
[PASS] m32-emulator-api has no WIE dependency
[PASS] m32-wie-adapter -> m32-emulator-api
[PASS] m32-wie-adapter -> wie_backend
[PASS] m32-desktop has no direct WIE dependency
[PASS] M32 session contract tests
[PASS] WIE session adapter compile bridge
[PASS] Existing emulator API dependency boundary
Emulator session contract verification passed.
```

Exit code:

```text
0
```

PASS.

## Cargo Boundary

```text
m32-wie-adapter
├── m32-emulator-api
└── wie_backend @ f0513eb758c02736981f545ad030eed937d55f3e
```

`m32-emulator-api` remains dependency-free and has no WIE dependency.

PASS.

## Workspace Tests

Actual tests:

- `m32-desktop`: 9 passed
- `m32-domain`: 3 passed
- `m32-emulator-api`: 6 passed
- `m32-test-fixtures`: 2 passed
- `m32-wie-adapter`: 5 passed

Minimum total:

```text
25 tests passed
```

## Quality Gates

```text
cargo fmt --all -- --check
exit 0

cargo clippy --workspace --all-targets -- -D warnings
exit 0

cargo test --workspace
exit 0

cargo check --workspace --all-targets
exit 0

git diff --check
exit 0
```

PASS.

Windows LF→CRLF notices from `git diff --check` were warnings only; exit code was `0`.

## 판정

`0.0.2-T003 = DONE`

M32 now owns the active emulator-session lifecycle and tick error boundary while the concrete
`wie_backend::Emulator` remains private to `m32-wie-adapter`.

## Next Exact Task

`0.0.2-T004`

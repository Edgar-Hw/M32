# M32 Task Evidence — 0.0.3-T009

- Task: `0.0.3-T009`
- Version: `0.0.3 First Frame`
- Area: First-frame Integration Verifier
- Status: DONE
- Rust: `1.98.0`
- Target: `x86_64-pc-windows-msvc`
- WIE revision: `f0513eb758c02736981f545ad030eed937d55f3e`
- RustJava base revision: `ba5797b8eb4cf376fdd63129903d319d1d7acf98`

## Goal

Create one canonical integration verifier for the complete First Frame contract established by
T001 through T008.

T009 intentionally adds no new runtime behavior.

## Canonical First Frame Outcomes

The integration verifier locks all three bounded-wait outcomes:

```text
SUCCESS
-> real guest-generated RgbaFrame

TIMEOUT
-> healthy Running guest
-> no frame within bounded wait

FAULT
-> backend/session failure before frame
-> failure must not be mislabeled as timeout
```

All three branches passed.

## Locked Fixture Identities

### RunningMidlet

```text
JAD SHA-256
e02fba5850f154913d1ed45d845c63f4cad53a2f1d66d348f5046a83c56a1ae7

JAR SHA-256
2521a16329f92ec6eaf07a2d9bc379713261c00146a722c14b5dd0aab5bb465d
```

### PaintMidlet / PaintCanvas

```text
JAD SHA-256
7fe94a7cb014c40367ee2139d25cf4e1c3cde37d8e9d73bcb1277a4a1f9efc33

JAR SHA-256
717063ece69306b9338da722d57f14815987c6a044f738da3b2e95373a9f8b5a
```

All hashes matched.

## Canonical Source Markers

Verified:

```text
FirstFrameCaptureDisplayHost
DeterministicAdvancingClock
FirstFrameWaitError
SessionFault
Timeout
first_frame_paint_fixture_ticks_until_guest_frame_is_captured
first_frame_paint_fixture_locks_exact_rgba8_dimensions_and_content
first_frame_wait_times_out_cleanly_when_running_midlet_never_paints
first_frame_wait_reports_backend_fault_before_timeout
```

PASS.

## T001-T008 Chain

The T009 verifier invoked the complete T008 verifier chain.

This recursively reran:

```text
T008 timeout/failure boundary
T007 exact RGBA8 content
T006 real tick-until-first-frame
T005 deterministic Canvas/Paint fixture
T004 FirstFrameCaptureDisplayHost
T003 positive boot
T002 deterministic bootable fixture
T001 explicit JAD+JAR launch
Core Adapter verification chain
RustJava compatibility verification chain
```

PASS.

## Adapter Regression Suite

```text
running 58 tests

test result: ok. 58 passed; 0 failed
```

PASS.

## Emulator API Regression Suite

```text
running 29 tests

test result: ok. 29 passed; 0 failed
```

PASS.

## Canonical Integration Verifier

Final output:

```text
[PASS] SUCCESS branch: real guest-generated exact RGBA8 frame
[PASS] TIMEOUT branch: healthy Running guest without frame
[PASS] FAULT branch: backend failure before timeout
[PASS] exact 176x220 / 154880-byte / 38720-pixel content contract
[PASS] capture host first-frame immutability
[PASS] deterministic executor-time repaint scheduling
[PASS] locked First Frame fixture identities
[PASS] complete T001-T008 verification chain
[PASS] full m32-wie-adapter regression suite
[PASS] full m32-emulator-api regression suite
First-frame canonical integration verification passed.
```

Exit code:

```text
0
```

PASS.

## T009 Quality Checks

```text
cargo fmt --all -- --check
exit 0

git diff --check
exit 0
```

PASS.

No Rust source was changed by T009.

## Repository Scope

Final status:

```text
?? docs/test/WIE_FIRST_FRAME_INTEGRATION_VERIFIER.md
?? scripts/verify-wie-first-frame-integration.ps1
```

No Cargo.lock change.

No runtime code change.

No WIE/RustJava change.

No guest fixture change.

## 판정

```text
0.0.3-T009 = DONE
```

## Next Task

```text
0.0.3-T010
First Frame Version Close / Gates
```

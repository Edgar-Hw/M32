# M32 Task Evidence — 0.0.4-T003

- Task: `0.0.4-T003`
- Area: Exact M32 -> pinned WIE Key/Event Mapping
- Status: DONE
- Rust: `1.98.0`
- Target: `x86_64-pc-windows-msvc`
- WIE revision: `f0513eb758c02736981f545ad030eed937d55f3e`
- RustJava base revision: `ba5797b8eb4cf376fdd63129903d319d1d7acf98`
- Bundle: `0.0.4 Input / Bundle A`


## Goal

Map the stable M32 input contract exactly onto the pinned WIE backend event surface.

## Exact Key Mapping

All 24 `M32Key` values are explicitly mapped to the corresponding pinned WIE `KeyCode`.

No numeric cast, fallback arm, or backend type leakage is used.

Examples:

```text
M32Key::Up        -> KeyCode::UP
M32Key::Ok        -> KeyCode::OK
M32Key::LeftSoft  -> KeyCode::LEFT_SOFT_KEY
M32Key::Num0      -> KeyCode::NUM0
M32Key::Num9      -> KeyCode::NUM9
M32Key::Hash      -> KeyCode::HASH
M32Key::Star      -> KeyCode::STAR
```

## Exact Phase Mapping

```text
KeyDown   -> wie_backend::Event::Keydown
KeyUp     -> wie_backend::Event::Keyup
KeyRepeat -> wie_backend::Event::Keyrepeat
```

`WieSession::handle_input()` forwards the mapped event into the pinned backend.

## Tests

```text
m32_key_maps_exactly_to_pinned_wie_key_code ... ok
m32_input_event_maps_press_release_repeat_to_wie_event ... ok
wie_session_forwards_m32_input_events_to_pinned_backend ... ok
```

## Bundle Validation

`m32-wie-adapter`:

```text
61 passed
0 failed
```

Bundle A verifier:

```text
[PASS] T003 exact M32Key -> pinned WIE KeyCode mapping
[PASS] T003 KeyDown -> WIE Keydown
[PASS] T003 KeyUp -> WIE Keyup
[PASS] T003 KeyRepeat -> WIE Keyrepeat
[PASS] T003 WieSession forwards input into pinned backend
```

Previous First Frame integration chain also remained green.

## Repository Scope

```text
M  crates/m32-emulator-api/src/lib.rs
M  crates/m32-wie-adapter/src/lib.rs
?? docs/test/WIE_INPUT_BUNDLE_A_FOUNDATION.md
?? scripts/verify-wie-input-bundle-a.ps1
```

No Cargo.lock/dependency/WIE/RustJava change.

## 판정

```text
0.0.4-T003 = DONE
```

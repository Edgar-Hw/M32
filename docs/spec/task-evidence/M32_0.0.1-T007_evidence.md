# M32 Task Evidence — 0.0.1-T007

- Task: `0.0.1-T007`
- Area: fixtures
- Status: DONE
- Rust: `1.98.0`
- Target: `x86_64-pc-windows-msvc`

## 적용 내용

- Synthetic fixture 정책 수립
- Fixture manifest schema v1 추가
- 자체 작성 J2ME source fixture 3개 추가
- 자체 생성 malformed binary fixture 3개 추가
- Fixture SHA-256 검증 PowerShell script 추가
- `m32-test-fixtures` baseline fixture registry 및 unit test 추가

## Fixture Manifest 검증

```text
Fixture manifest verification passed.
Schema: 1
Fixtures: 6
```

모든 fixture의 실제 SHA-256과 manifest SHA-256이 일치함.

검증된 fixture:

- `j2me-hello-source`
- `j2me-input-source`
- `j2me-audio-source`
- `malformed-truncated-zip`
- `malformed-truncated-class`
- `malformed-truncated-png`

모든 tracked fixture의 `redistribution_status`는 `redistributable`.

## Rust Unit Tests

최종 workspace test에서 `m32-test-fixtures`의 실제 unit test 2개 실행 확인:

```text
running 2 tests
test tests::baseline_fixture_ids_are_kebab_case_ascii ... ok
test tests::baseline_fixture_ids_are_unique ... ok

test result: ok. 2 passed; 0 failed
```

PASS.

## Quality Gates

```text
cargo fmt --all -- --check
exit 0

cargo clippy --workspace --all-targets -- -D warnings
exit 0

cargo test --workspace
exit 0

cargo check --workspace
exit 0

git diff --check
exit 0
```

PASS.

`git diff --check`에서 Windows working tree의 LF→CRLF 변환 예정 경고가 출력되었으나
exit code는 `0`이며 whitespace 오류는 아님. Repository EOL 정책은 별도 Foundation
변경 항목에서 필요 시 다룬다.

## 판정

`0.0.1-T007 = DONE`

상용 게임 파일 없이도 M32 parser/import/security/J2ME 경로를 확장할 수 있는
fixture governance와 검증 기반이 구축됨.

## Next Exact Task

`0.0.1-T008`

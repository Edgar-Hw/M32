# M32 Task Evidence — 0.0.6-T003

- Task: `0.0.6-T003`
- Area: Persistent Guest Filesystem
- Version: `0.0.6 Storage / Persistence`
- Bundle: `Bundle A`
- Rust: `1.98.0`
- Target: `x86_64-pc-windows-msvc`
- Status: DONE


## Contract

Persistent guest files are stored beneath:

```text
%LOCALAPPDATA%\M32\guest-files
```

Supported semantics:

```text
exists
size
read
write
truncate
create-on-write
create-on-truncate
EOF read -> Some(0)
missing read -> None
write past EOF -> zero-filled gap
truncate growth -> zero-filled extension
```

Tests:

```text
filesystem_write_read_size_and_exists_round_trip ... ok
filesystem_write_past_eof_zero_fills_gap ... ok
filesystem_truncate_shrinks_extends_and_creates ... ok
filesystem_missing_and_eof_read_semantics_are_exact ... ok
```

Verifier:

```text
[PASS] T003 persistent guest filesystem semantics
[PASS] T003 write/truncate zero-extension behavior
```

```text
0.0.6-T003 = DONE
```

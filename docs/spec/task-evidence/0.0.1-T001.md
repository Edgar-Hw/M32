# 0.0.1-T001 Evidence

Status: PENDING_LOCAL_VALIDATION

## Scope

- Root Cargo workspace created.
- Cargo resolver fixed to `2`.
- Workspace package edition fixed to Rust `2024`.
- Workspace contains 14 members: desktop app 1 + v1.0 core crates 13.
- v1.1+ crates (`m32-replay`, `m32-community`, `m32-phone-os`, `m32-manual`) are intentionally not created yet.
- Every library crate has a minimal `src/lib.rs` so it can compile once the Rust toolchain is installed.
- The desktop app has a minimal `src/main.rs`.

## Canonical members

1. `apps/m32-desktop`
2. `crates/m32-domain`
3. `crates/m32-ui`
4. `crates/m32-emulator-api`
5. `crates/m32-wie-adapter`
6. `crates/m32-library`
7. `crates/m32-dna`
8. `crates/m32-doctor`
9. `crates/m32-display`
10. `crates/m32-audio`
11. `crates/m32-input`
12. `crates/m32-archive`
13. `crates/m32-timeline`
14. `crates/m32-test-fixtures`

## Required local validation

Run from repository root after completing `0.0.1-T002` toolchain setup, or immediately if Rust 1.98.0 is already available locally:

```powershell
cargo metadata --no-deps --format-version 1
cargo check --workspace
```

Expected:

- Both commands exit with code `0`.
- `cargo metadata` lists exactly 14 workspace members.
- No crate outside the canonical list is present.

## Environment note

The artifact-generation environment used to prepare this scaffold does not have `rustc/cargo` installed, so the acceptance command must be executed on the developer Windows machine before this Task is marked DONE.

# M32 0.1.0-T008 Evidence — Integrated Tick / Redraw / Input / Audio / Storage Loop

Status before verification: IMPLEMENTED / NOT YET ACCEPTED

## Automated integration path

One desktop composition test drives the canonical First Playable fixture through:

```text
local JAD+JAR launch
-> SessionState::Running
-> 176x220 guest frame
-> M32Key input through GuestInputController
-> Java Canvas keyPressed
-> RMS save sentinel
-> MMAPI audio command delegated into RealtimeGuestAudioHost
-> repaint
-> visibly different next RGBA8 frame
```

The test uses the real product composition root but disables only the physical CPAL device opening, because CI cannot require a physical default audio device. The realtime audio host and WIE audio dispatch path remain real.

# M32 Task Evidence — 0.0.3-T005

- Task: `0.0.3-T005`
- Version: `0.0.3 First Frame`
- Area: Deterministic Canvas/Paint Fixture
- Status: DONE
- Rust: `1.98.0`
- Target: `x86_64-pc-windows-msvc`
- WIE revision: `f0513eb758c02736981f545ad030eed937d55f3e`
- RustJava base revision: `ba5797b8eb4cf376fdd63129903d319d1d7acf98`

## Goal

Introduce a deterministic M32-owned LCDUI guest fixture for the real First Frame runtime path.

T005 locks guest bytes and the intended paint contract only.

Actual tick-to-frame execution remains T006.

## Fixture

Runtime guest classes:

```text
m32.PaintMidlet
m32.PaintCanvas
```

Launch descriptor:

```text
MIDlet-1: M32 Paint,,m32.PaintMidlet
```

`PaintMidlet.startApp()`:

```java
Display.getDisplay(this).setCurrent(new PaintCanvas());
System.out.println("M32_FIRST_FRAME_CANVAS_READY");
System.out.flush();
```

`PaintCanvas.paint(Graphics)`:

```java
graphics.setColor(0x0E1114);
graphics.fillRect(0, 0, 176, 220);

graphics.setColor(0xD14A36);
graphics.fillRect(0, 0, 16, 16);
```

Locked visual intent:

```text
logical size: 176 x 220
background:   #0E1114 (M32 BG0)
marker:       #D14A36 (M32 RED), top-left 16 x 16
```

No text rendering, image resource, font, interpolation, or external asset is needed for the paint
pattern.

## Compile-time LCDUI Stubs

Added synthetic compile-time-only source stubs:

```text
javax/microedition/lcdui/Displayable.java
javax/microedition/lcdui/Display.java
javax/microedition/lcdui/Canvas.java
javax/microedition/lcdui/Graphics.java
```

The runtime JAR does not include those stubs.

Their method descriptors were aligned with the pinned WIE LCDUI surface used by the fixture:

```text
Display.getDisplay(MIDlet) -> Display
Display.setCurrent(Displayable) -> void
Canvas::<init>() -> void
Canvas.paint(Graphics) -> void
Graphics.setColor(int) -> void
Graphics.fillRect(int,int,int,int) -> void
```

## Locked Hashes

```text
JAD
7fe94a7cb014c40367ee2139d25cf4e1c3cde37d8e9d73bcb1277a4a1f9efc33

JAR
717063ece69306b9338da722d57f14815987c6a044f738da3b2e95373a9f8b5a

m32/PaintCanvas.class
87660a63443067de1c66d49c4246d60f575d0512a19c797ca235aa62dd1aa3c4

m32/PaintMidlet.class
85ebbc6796bd0c2dc9bd9192e931da3e9d714ef3e5d404e3543769267a73c706
```

JAR size:

```text
1243 bytes
```

Both guest classes:

```text
classfile minor = 0
classfile major = 52
```

## Targeted Tests

```text
first_frame_paint_fixture_locks_canvas_and_pixel_pattern_contract ... ok
first_frame_paint_fixture_constructs_ready_j2me_session ... ok
```

Each:

```text
1 passed
0 failed
exit 0
```

## Adapter Tests

```text
running 53 tests

test result: ok. 53 passed; 0 failed
```

PASS.

## Emulator API Tests

```text
running 29 tests

test result: ok. 29 passed; 0 failed
```

PASS.

## T005 Verifier

```text
[PASS] deterministic PaintMidlet/PaintCanvas JAD/JAR
[PASS] locked T005 JAD/JAR SHA-256
[PASS] explicit m32.PaintMidlet launch entry
[PASS] Display.getDisplay -> setCurrent Canvas contract
[PASS] 176x220 M32 BG0 full-frame paint contract
[PASS] 16x16 M32 RED marker paint contract
[PASS] Ready pinned J2ME constructor path
[PASS] previous capture-host/positive-boot verification chain
Deterministic Canvas/Paint fixture verification passed.
```

Exit code:

```text
0
```

PASS.

## Workspace Tests

Actual M32 unit tests:

- `m32-desktop`: 9
- `m32-domain`: 3
- `m32-emulator-api`: 29
- `m32-test-fixtures`: 2
- `m32-wie-adapter`: 53

Minimum total:

```text
96 tests passed
```

## Quality Gates

```text
cargo fmt --all -- --check
exit 0

cargo check -p m32-wie-adapter
exit 0

cargo clippy --workspace --all-targets -- -D warnings
exit 0

cargo test --workspace
exit 0

cargo check --workspace --all-targets
exit 0

git diff --check
exit 0
```

PASS.

LF->CRLF warning was non-blocking; exit code remained `0`.

## Repository Scope

Final status contained only T005 files:

```text
 M crates/m32-wie-adapter/src/lib.rs
?? crates/m32-wie-adapter/test-fixtures/j2me-first-frame-paint.jad
?? crates/m32-wie-adapter/test-fixtures/j2me-first-frame-paint.jar
?? crates/m32-wie-adapter/test-fixtures/src/javax/microedition/lcdui/
?? crates/m32-wie-adapter/test-fixtures/src/m32/PaintCanvas.java
?? crates/m32-wie-adapter/test-fixtures/src/m32/PaintMidlet.java
?? docs/test/WIE_FIRST_FRAME_CANVAS_PAINT_FIXTURE.md
?? scripts/verify-wie-first-frame-canvas-paint-fixture.ps1
```

No Cargo.lock change.

No RustJava source change.

## 판정

```text
0.0.3-T005 = DONE
```

## Next Task

```text
0.0.3-T006
Tick-until-first-frame Harness
```

# M32 Task Evidence — 0.0.3-T008

- Task: `0.0.3-T008`
- Version: `0.0.3 First Frame`
- Area: First-frame Timeout/Failure Boundary
- Status: DONE
- Rust: `1.98.0`
- Target: `x86_64-pc-windows-msvc`
- WIE revision: `f0513eb758c02736981f545ad030eed937d55f3e`
- RustJava base revision: `ba5797b8eb4cf376fdd63129903d319d1d7acf98`

## Goal

Define an explicit negative boundary around the real First Frame harness.

The bounded wait now distinguishes exactly three outcomes:

```text
frame captured
backend/session fault before frame
healthy session timeout without frame
```

Backend failure must not be mislabeled as first-frame timeout.

A healthy MIDlet that never paints must not be mislabeled as a backend fault.

## Test-only Wait Result

T006's wait path was factored into:

```text
try_tick_until_first_captured_frame(...)
tick_until_first_captured_frame(...)
```

The lower-level helper returns:

```text
Ok(RgbaFrame)

Err(SessionFault {
    tick,
    EmulatorSessionError
})

Err(Timeout {
    max_ticks,
    redraws,
    presents,
    SessionState
})
```

The existing success-path wrapper keeps panic-on-test-failure behavior.

No public emulator API changed.

## Healthy No-frame Timeout

Fixture:

```text
m32.RunningMidlet
```

This guest was already locked by T003.

Observed contract:

```text
MIDlet.startApp executes
stdout contains M32_FIRST_FRAME_BOOT_OK
SessionState = Running
redraws = 0
presents = 0
first_frame = None
```

The wait bound reuses the existing First Frame bound:

```text
512 ticks
```

Expected result:

```text
Timeout
max_ticks = 512
state = Running
```

Targeted test:

```text
first_frame_wait_times_out_cleanly_when_running_midlet_never_paints ... ok
```

Result:

```text
1 passed
0 failed
exit 0
```

## Backend Fault Precedence

Fixture:

```text
j2me-core-smoke-missing-main.jar
```

Expected:

```text
SessionFault
error.code = BackendTickFailed
SessionState = Faulted
first_frame = None
present_count = 0
```

The fault must be returned before timeout.

Targeted test:

```text
first_frame_wait_reports_backend_fault_before_timeout ... ok
```

Result:

```text
1 passed
0 failed
exit 0
```

This locks:

```text
backend/session fault != first-frame timeout
```

## Success Outcome Remains Green

The real PaintMidlet path remains:

```text
Ok(RgbaFrame)
```

through:

```text
PaintMidlet
-> Display.setCurrent
-> Event::Redraw
-> MIDP EventQueue
-> PaintCanvas.paint
-> WIE screen.paint
-> WieScreenAdapter
-> FirstFrameCaptureDisplayHost
```

T007 exact RGBA8 verification reran successfully through the T008 verifier chain.

## Adapter Tests

```text
running 58 tests

test result: ok. 58 passed; 0 failed
```

PASS.

## Emulator API Tests

```text
running 29 tests

test result: ok. 29 passed; 0 failed
```

PASS.

## T008 Verifier

```text
[PASS] bounded healthy-session first-frame timeout
[PASS] RunningMidlet reaches Running without inventing a frame
[PASS] timeout diagnostics lock max_ticks/redraws/presents/state
[PASS] no-frame timeout preserves first_frame=None
[PASS] backend tick fault is reported before timeout
[PASS] BackendTickFailed identity preserved
[PASS] faulted session remains Faulted
[PASS] backend failure is not mislabeled as first-frame timeout
[PASS] previous exact RGBA8/real first-frame verification chain
First-frame timeout/failure boundary verification passed.
```

Exit code:

```text
0
```

PASS.

The T008 verifier reran the complete T007/T006/T005/T004/T003/T002/T001/Core Adapter chain.

## Workspace Tests

Actual M32 unit tests:

- `m32-desktop`: 9
- `m32-domain`: 3
- `m32-emulator-api`: 29
- `m32-test-fixtures`: 2
- `m32-wie-adapter`: 58

Minimum total:

```text
101 tests passed
```

## Quality Gates

```text
cargo fmt --all
exit 0

cargo fmt --all -- --check
exit 0

cargo check -p m32-wie-adapter
exit 0

cargo clippy --workspace --all-targets -- -D warnings
exit 0

cargo test --workspace
exit 0

cargo check --workspace --all-targets
exit 0

git diff --check
exit 0
```

PASS.

LF->CRLF warning remained non-blocking.

## Repository Scope

Final status:

```text
 M crates/m32-wie-adapter/src/lib.rs
?? docs/test/WIE_FIRST_FRAME_TIMEOUT_FAILURE_BOUNDARY.md
?? scripts/verify-wie-first-frame-timeout-failure.ps1
```

No Cargo.lock change.

No WIE/RustJava change.

No guest fixture change.

## 판정

```text
0.0.3-T008 = DONE
```

## Next Task

```text
0.0.3-T009
First-frame Integration Verifier
```

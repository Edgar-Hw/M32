# M32 Task Evidence — 0.0.1-T008

- Task: `0.0.1-T008`
- Area: build provenance
- Status: DONE
- Rust: `1.98.0`
- Target: `x86_64-pc-windows-msvc`

## 적용 내용

- `m32-domain::BuildInfo` 추가
- Product Spec / Spec Bundle / WIE baseline 고정값 노출
- build-time Git HEAD 수집
- build-time rustc version 수집
- build target / profile 수집
- `m32-desktop`에서 BuildInfo 출력
- `m32-desktop -> m32-domain` path dependency 연결

## 실제 검증

### Formatting

```text
cargo fmt --all -- --check
$LASTEXITCODE
0
```

PASS.

### Domain Unit Tests

```text
running 3 tests
test build_info::tests::compile_time_build_fields_are_present ... ok
test build_info::tests::pinned_rust_and_target_are_visible ... ok
test build_info::tests::fixed_spec_baselines_match_locked_values ... ok

test result: ok. 3 passed; 0 failed
```

PASS.

### BuildInfo Runtime Output

검증된 값:

```text
app_version=0.0.0
product_spec_version=1.0.0
spec_bundle_version=1.0.1
git_commit=9ba46b279c4077c0dc78b3a8ed02c67ac47d159c
wie_commit=f0513eb758c02736981f545ad030eed937d55f3e
rust_version=rustc 1.98.0 (88d9e12ae 2026-08-18)
target=x86_64-pc-windows-msvc
build_profile=debug
```

`git_commit`과 실제 `git rev-parse HEAD` 비교 결과:

```text
True
```

Product Spec, Spec Bundle, WIE revision, Rust version, target, build profile 검사도 모두 `True`.

### Quality Gates

```text
cargo clippy --workspace --all-targets -- -D warnings
exit 0

cargo test --workspace
exit 0

cargo check --workspace
exit 0

git diff --check
exit 0
```

PASS.

전체 workspace test에서:

- `m32-domain`: 3 tests passed
- `m32-test-fixtures`: 2 tests passed

총 최소 5개 unit test가 실제 실행됨.

Windows working tree의 LF→CRLF 예정 경고가 일부 출력되었으나 `git diff --check`
exit code는 `0`이며 whitespace 오류는 아님.

## 판정

`0.0.1-T008 = DONE`

M32 binary가 자신의 app/spec/git/WIE/rust/target/profile provenance를 구조적으로
노출할 수 있는 기반이 구축됨.

## Next Exact Task

`0.0.1-T009`

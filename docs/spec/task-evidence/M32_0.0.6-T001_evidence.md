# M32 Task Evidence — 0.0.6-T001

- Task: `0.0.6-T001`
- Area: Persistent Storage Root / SQLite Policy
- Version: `0.0.6 Storage / Persistence`
- Bundle: `Bundle A`
- Rust: `1.98.0`
- Target: `x86_64-pc-windows-msvc`
- Status: DONE


## Contract

Persistent storage is rooted beneath the already locked M32 Windows application root:

```text
%LOCALAPPDATA%\M32
```

Storage-owned paths:

```text
storage.sqlite3
guest-files\
```

SQLite policy:

```text
schema version = 1
journal_mode   = WAL
foreign_keys   = ON
busy_timeout   = 2000ms
```

Pinned database dependency:

```text
rusqlite = =0.37.0
features = bundled
```

Test:

```text
sqlite_policy_is_wal_foreign_keys_busy_timeout_2000_and_schema_v1 ... ok
storage_paths_are_rooted_under_m32_root ... ok
```

Verifier:

```text
[PASS] T001 %LOCALAPPDATA%\M32-compatible storage root layout
[PASS] T001 SQLite WAL / FK / 2000ms busy-timeout / schema-v1 policy
[PASS] T001 rusqlite pinned exactly to 0.37.0 with bundled SQLite
```

```text
0.0.6-T001 = DONE
```

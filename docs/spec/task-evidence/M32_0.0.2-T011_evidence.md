# M32 Task Evidence — 0.0.2-T011

- Task: `0.0.2-T011`
- Version: `0.0.2 Core Adapter`
- Area: WIE J2ME Session Factory
- Status: DONE
- Rust: `1.98.0`
- Target: `x86_64-pc-windows-msvc`
- Emulator API schema: `1`
- WIE revision: `f0513eb758c02736981f545ad030eed937d55f3e`
- RustJava revision: `ba5797b8eb4cf376fdd63129903d319d1d7acf98`

## 적용 내용

- `wie_j2me`를 `m32-wie-adapter`의 exact pinned direct dependency로 추가
- generic M32 `EmulatorSessionCreateError` / `SessionCreateErrorCode` 추가
- `create_j2me_jar_session(...)` 추가
- `WiePlatformAdapter`를 `Box<dyn wie_backend::Platform>`으로 소유권 이전
- JAR filename + JAR bytes를 pinned `J2MEEmulator::from_jar(...)`에 전달
- concrete `J2MEEmulator`를 기존 `WieSession` 뒤로 숨김
- factory 결과를 `Box<dyn EmulatorSession>` / `SessionState::Ready`로 반환
- M32 API에 WIE concrete type 누출 없음

## FIX1 — RustJava upstream resolution

Direct `wie_j2me` reachability exposed an upstream reproducibility issue:

```text
no matching package named `java_class_proto` found
```

Pinned WIE's own `Cargo.lock` records RustJava at:

```text
ba5797b8eb4cf376fdd63129903d319d1d7acf98
```

M32 therefore vendors that exact source snapshot into:

```text
third_party/rustjava
```

and path-patches the six packages required by the pinned WIE JVM graph:

```text
classfile
java_class_proto
java_constants
java_runtime
jvm
jvm_rust
```

The vendor bootstrap verifies exact Git revision, package manifests, upstream LICENSE, and removes
the nested `.git` directory.

## FIX2 — RustJava workspace isolation

Vendored RustJava crates use `*.workspace = true` inheritance.

M32 therefore excludes:

```text
third_party/rustjava
```

from the outer M32 workspace, preserving RustJava's own nested workspace root and its
`[workspace.package]` / `[workspace.dependencies]` inheritance.

Final vendor verification:

```text
[PASS] vendor directory
[PASS] exact RustJava revision
[PASS] required package manifests
[PASS] java_class_proto restored from locked revision
[PASS] upstream LICENSE
[PASS] no nested .git
[PASS] RustJava nested workspace manifest
[PASS] RustJava workspace.package inheritance metadata
[PASS] RustJava nested workspace Cargo metadata
```

Final workspace-boundary verification:

```text
[PASS] M32 workspace excludes vendored RustJava
[PASS] vendored RustJava retains its own workspace root
[PASS] RustJava workspace inheritance parses independently
```

## Final Emulator API Tests

```text
running 29 tests
...
test tests::session_create_error_implements_std_error_contract ... ok
test tests::session_create_error_keeps_stable_code_and_message ... ok

test result: ok. 29 passed; 0 failed
```

PASS.

## Final WIE Adapter Tests

```text
running 38 tests
...
test tests::pinned_j2me_emulator_implements_wie_emulator_contract ... ok
test tests::j2me_jar_factory_constructs_ready_m32_session ... ok
test tests::session_create_error_maps_to_stable_m32_code ... ok

test result: ok. 38 passed; 0 failed
```

PASS.

The `j2me_jar_factory_constructs_ready_m32_session` test calls the actual pinned
`wie_j2me::J2MEEmulator::from_jar(...)` constructor and returns an M32 `Ready` session.

## J2ME Factory Verifier

```text
[PASS] pinned direct wie_j2me dependency
[PASS] J2MEEmulator -> WIE Emulator contract
[PASS] WiePlatformAdapter ownership transfer into J2ME constructor
[PASS] JAR filename/byte constructor path
[PASS] Ready M32 session wrapping concrete J2ME emulator
[PASS] stable M32 session-create error mapping
[PASS] existing emulator API dependency boundary
WIE J2ME session factory verification passed.
```

Exit code:

```text
0
```

PASS.

## Dependency Boundary

Adapter depth-1 graph includes:

```text
async-trait v0.1.92
m32-emulator-api
tracing v0.1.41
wie_backend @ f0513eb758c02736981f545ad030eed937d55f3e
wie_j2me @ f0513eb758c02736981f545ad030eed937d55f3e
wie_util @ f0513eb758c02736981f545ad030eed937d55f3e
```

RustJava transitive packages resolve from local paths under:

```text
third_party/rustjava
```

`m32-emulator-api` remains dependency-free from WIE.

PASS.

## Cargo.lock

Dependency resolution completed successfully and locked the newly reachable J2ME/JVM graph.

Observed additions include:

```text
wie_j2me
wie_jvm_support
wie_midp
classfile
java_class_proto
java_constants
java_runtime
jvm
jvm_rust
```

plus required registry dependencies.

`Cargo.lock` change is intentional and required.

## Workspace Tests

Actual M32 tests:

- `m32-desktop`: 9
- `m32-domain`: 3
- `m32-emulator-api`: 29
- `m32-test-fixtures`: 2
- `m32-wie-adapter`: 38

Minimum total:

```text
81 tests passed
```

Vendored RustJava is intentionally excluded from the M32 workspace test count.

## Quality Gates

```text
cargo fmt --all -- --check
exit 0

cargo clippy --workspace --all-targets -- -D warnings
exit 0

cargo test --workspace
exit 0

cargo check --workspace --all-targets
exit 0

git diff --check
exit 0
```

PASS.

LF→CRLF messages from Git were warnings only; exit code remained `0`.

## 판정

`0.0.2-T011 = DONE`

M32 can now construct a real pinned WIE J2ME emulator from M32 host services and supplied JAR bytes,
then hide that concrete runtime behind the stable M32 `EmulatorSession` boundary.

## Next Exact Task

`0.0.2-T012`

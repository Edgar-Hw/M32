# M32 Task Evidence — 0.0.2-T002

- Task: `0.0.2-T002`
- Version: `0.0.2 Core Adapter`
- Area: emulator API boundary
- Status: DONE
- Rust: `1.98.0`
- Target: `x86_64-pc-windows-msvc`
- Emulator API schema: `1`

## 적용 내용

- `m32-emulator-api`에 M32-owned backend identity contract 추가
- `BackendDescriptor` 추가
- `EmulatorBackend` trait 추가
- `WieBackendAdapter`가 M32 `EmulatorBackend`를 구현
- WIE backend identity를 M32 API를 통해 노출
- WIE concrete types를 M32 application-facing API에서 분리
- Cargo metadata 기반 dependency-boundary verifier 추가

## Emulator API Unit Tests

```text
running 3 tests
test tests::api_schema_is_version_one ... ok
test tests::backend_descriptor_keeps_identity_fields ... ok
test tests::backend_trait_is_implementation_agnostic ... ok

test result: ok. 3 passed; 0 failed
```

PASS.

## WIE Adapter Unit Tests

```text
running 3 tests
test tests::adapter_exposes_wie_identity_through_m32_api ... ok
test tests::pinned_wie_backend_options_api_is_available ... ok
test tests::upstream_revision_matches_locked_baseline ... ok

test result: ok. 3 passed; 0 failed
```

PASS.

## Dependency Boundary Verification

Actual verifier output:

```text
[PASS] m32-emulator-api has no WIE dependency
[PASS] m32-wie-adapter -> m32-emulator-api
[PASS] m32-wie-adapter -> wie_backend
[PASS] m32-desktop has no direct WIE dependency
Emulator API boundary verification passed.
```

Exit code:

```text
0
```

PASS.

## Cargo Tree

Adapter direct dependencies:

```text
m32-wie-adapter
├── m32-emulator-api
└── wie_backend @ f0513eb758c02736981f545ad030eed937d55f3e
```

`m32-emulator-api` tree:

```text
m32-emulator-api
```

No WIE dependency.

PASS.

## Workspace Tests

Actual tests:

- `m32-desktop`: 9 passed
- `m32-domain`: 3 passed
- `m32-emulator-api`: 3 passed
- `m32-test-fixtures`: 2 passed
- `m32-wie-adapter`: 3 passed

Minimum total:

```text
20 tests passed
```

## Quality Gates

```text
cargo fmt --all -- --check
exit 0

cargo clippy --workspace --all-targets -- -D warnings
exit 0

cargo test --workspace
exit 0

cargo check --workspace --all-targets
exit 0

git diff --check
exit 0
```

PASS.

Windows LF→CRLF notices from `git diff --check` were warnings only; exit code was `0`.

## 판정

`0.0.2-T002 = DONE`

M32 now owns the application-facing emulator backend identity contract while the concrete WIE
dependency remains isolated inside `m32-wie-adapter`.

## Next Exact Task

`0.0.2-T003`

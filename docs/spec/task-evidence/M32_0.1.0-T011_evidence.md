# M32 0.1.0-T011 Evidence — Session Shutdown / Fault Boundary

Status before verification: IMPLEMENTED / NOT YET ACCEPTED

## Product lifecycle behavior

```text
Window focus lost -> PlayableRuntime::set_paused(true) -> existing 80ms audio pause fade
Window focus regained -> set_paused(false) -> resume
WIE Platform::exit -> M32 ExitHost -> explicit playable runtime stop -> event-loop exit
Window close -> explicit playable runtime stop -> event-loop exit
Backend tick fault -> error remains BackendTickFailed -> runtime stop -> fatal desktop result
PlayableRuntime drop -> pause request -> CPAL stream/session/storage handles released by ownership
```

Automated proof covers the product exit-host signal, WIE Platform::exit delegation, pause/resume without destroying the runtime, backend-fault identity, Windows desktop lifecycle compilation, and existing storage/audio lifetime regression gates.

## Factual pinned-WIE constraint

The locked WIE revision `f0513eb758c02736981f545ad030eed937d55f3e` defines `javax.microedition.midlet.MIDlet.notifyDestroyed()` as a stub. Therefore a J2ME MIDlet calling `notifyDestroyed()` cannot currently generate `Platform::exit` without changing the pinned WIE implementation.

Per the locked 0.1.0 handoff rule allowing a factual API/repository constraint to adjust scope when documented, T011 does **not** claim guest `notifyDestroyed()` end-to-end exit support. It proves the M32 product/WIE exit boundary that is actually exposed by the pinned backend, while keeping the WIE/RustJava pins and dependency boundary unchanged. Guest `notifyDestroyed()` support remains a backend compatibility follow-up rather than being falsely marked as implemented.

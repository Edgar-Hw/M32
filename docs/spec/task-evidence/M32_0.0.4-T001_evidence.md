# M32 Task Evidence — 0.0.4-T001

- Task: `0.0.4-T001`
- Area: Stable M32Key / GuestInputEvent Contract
- Status: DONE
- Rust: `1.98.0`
- Target: `x86_64-pc-windows-msvc`
- WIE revision: `f0513eb758c02736981f545ad030eed937d55f3e`
- RustJava base revision: `ba5797b8eb4cf376fdd63129903d319d1d7acf98`
- Bundle: `0.0.4 Input / Bundle A`


## Goal

Define the backend-agnostic feature-phone guest key vocabulary owned by `m32-emulator-api`.

## Locked Key Surface

Exactly 24 keys:

```text
Up Down Left Right Ok
LeftSoft RightSoft
Clear Call Hangup
VolumeUp VolumeDown
Num0 Num1 Num2 Num3 Num4 Num5 Num6 Num7 Num8 Num9
Hash Star
```

The complete surface is exposed through:

```text
M32Key::ALL
```

## Locked Input Phases

```text
GuestInputEvent::KeyDown(M32Key)
GuestInputEvent::KeyUp(M32Key)
GuestInputEvent::KeyRepeat(M32Key)
```

No WIE type leaks through the emulator API.

## Tests

```text
m32_key_contract_covers_complete_pinned_feature_phone_surface ... ok
guest_input_event_preserves_key_and_phase ... ok
```

## Bundle Validation

`m32-emulator-api`:

```text
32 passed
0 failed
```

Bundle A verifier:

```text
[PASS] T001 exact 24-key M32Key surface
[PASS] T001 backend-agnostic KeyDown/KeyUp/KeyRepeat contract
```

## Quality Gates

Bundle A passed:

```text
cargo fmt --all -- --check
cargo check -p m32-emulator-api
cargo check -p m32-wie-adapter
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
cargo check --workspace --all-targets
git diff --check
```

## 판정

```text
0.0.4-T001 = DONE
```

# M32 Task Evidence — 0.0.6-T005

- Task: `0.0.6-T005`
- Area: Restart Persistence / Usage Accounting
- Version: `0.0.6 Storage / Persistence`
- Bundle: `Bundle A`
- Rust: `1.98.0`
- Target: `x86_64-pc-windows-msvc`
- Status: DONE


## Restart Contract

The persistence backend was destroyed and reconstructed against the same M32 root.

Verified across reopen:

```text
RMS bytes survive
RMS next_id survives
guest file bytes survive
different app_id cannot observe another app database
different AID cannot observe another guest file
```

Tests:

```text
database_persists_across_reopen ... ok
combined_storage_reopen_preserves_database_and_file_without_cross_game_leak ... ok
```

## Usage Contract

`usage(app_id)` sums persistent guest-record payload bytes owned by that exact application.

Test:

```text
usage_sums_record_payload_bytes_per_app ... ok
```

## Final Bundle A Baseline

```text
m32-storage 14 / 14
```

Final Bundle A verifier:

```text
M32 0.0.6 Storage Bundle A verification passed.
exit code 0
```

Quality/regression evidence from the same validation sequence:

```text
cargo fmt --all -- --check PASS
cargo clippy -p m32-storage --all-targets -- -D warnings PASS
cargo test -p m32-storage 14/14 PASS
workspace regression chain PASS
workspace all-target compile PASS
git diff --check PASS
previous 0.0.5 Audio version-close PASS
```

FIX1 only aligned code with Rust 1.98 Clippy:

```text
redundant map_err closure removed
manual NoopWake replaced with Waker::noop()
```

No storage behavior changed.

```text
0.0.6-T005 = DONE
0.0.6 Storage Bundle A = DONE
```

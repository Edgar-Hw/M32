# M32 Task Evidence — 0.0.3-T002

- Task: `0.0.3-T002`
- Version: `0.0.3 First Frame`
- Area: Deterministic Bootable MIDlet Fixture
- Status: DONE
- Rust: `1.98.0`
- Target: `x86_64-pc-windows-msvc`
- WIE revision: `f0513eb758c02736981f545ad030eed937d55f3e`

## Fixture

Files:

```text
crates/m32-wie-adapter/test-fixtures/j2me-first-frame-boot.jad
crates/m32-wie-adapter/test-fixtures/j2me-first-frame-boot.jar
crates/m32-wie-adapter/test-fixtures/src/m32/FirstFrameMidlet.java
crates/m32-wie-adapter/test-fixtures/src/javax/microedition/midlet/MIDlet.java
```

Locked hashes:

```text
JAD
a30e92605f738bdca0eeb2f7c694b87aa21fbf212215c652287fb50c8e9f745d

CLASS
27738f217b6180ba72d91aaaa5c063d32f100554c8b3d74751ff3ff359b2b7b3

JAR
9ff5c3a86d913f7f49453312773af6b1cc43f595674e1206bcb64268dc573b3c
```

Classfile:

```text
minor = 0
major = 52
```

The JAR contains only M32-owned synthetic guest code.

The `javax.microedition.midlet.MIDlet` Java source is compile-time scaffolding and is not packed into
the runtime JAR.

## FIX1

Initial fixture identity testing failed because the generated Rust assertion accidentally used:

```rust
b"PK\\x03\\x04"
```

instead of the actual ZIP local-file signature:

```rust
b"PK\x03\x04"
```

FIX1 changed only the test literal.

No JAD, JAR, Java source, production factory, dependency, or Cargo.lock bytes changed.

## Targeted Tests

```text
first_frame_boot_fixture_has_expected_container_and_class_identity ... ok
first_frame_boot_fixture_constructs_ready_j2me_session ... ok
```

Each targeted test:

```text
1 passed
0 failed
exit 0
```

## Adapter Tests

```text
running 44 tests

test result: ok. 44 passed; 0 failed
```

PASS.

## Emulator API Tests

```text
running 29 tests

test result: ok. 29 passed; 0 failed
```

PASS.

## T002 Verifier

```text
[PASS] deterministic JAD fixture
[PASS] deterministic JAR fixture
[PASS] locked JAD/JAR SHA-256
[PASS] M32-owned MIDlet source provenance
[PASS] explicit m32.FirstFrameMidlet entry point
[PASS] Java 8 classfile identity
[PASS] Ready pinned J2ME constructor path
[PASS] previous First Frame/Core Adapter verification chain
Deterministic bootable MIDlet fixture verification passed.
```

Exit code:

```text
0
```

PASS.

## Workspace Tests

Actual M32 tests:

- `m32-desktop`: 9
- `m32-domain`: 3
- `m32-emulator-api`: 29
- `m32-test-fixtures`: 2
- `m32-wie-adapter`: 44

Minimum total:

```text
87 tests passed
```

## Quality Gates

```text
cargo fmt --all -- --check
exit 0

cargo check -p m32-wie-adapter
exit 0

cargo clippy --workspace --all-targets -- -D warnings
exit 0

cargo test --workspace
exit 0

cargo check --workspace --all-targets
exit 0

git diff --check
exit 0
```

PASS.

LF->CRLF warning was non-blocking; exit code remained `0`.

## 판정

```text
0.0.3-T002 = DONE
```

## Next Task

```text
0.0.3-T003
Positive boot-to-Running smoke with guest startApp sentinel
```

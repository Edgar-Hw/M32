# M32 Task Evidence — 0.0.5-T009

- Task: `0.0.5-T009`
- Version: `0.0.5 Audio`
- Area: Windows CPAL Output + 60ms Target + 80ms Pause Fade
- Status: DONE

## Windows Output Contract

Pinned Windows output dependency:

```text
cpal = =0.18.2
```

Canonical requested stream:

```text
sample format  f32
channels       2
sample rate    48000 Hz
```

Latency policy:

```text
target         60ms
target frames  2880 @ 48kHz
```

When the device advertises a callback-buffer range containing 2880 frames, M32 requests that exact
size. Otherwise M32 uses the device default callback buffer. 60ms is therefore a target, not a false
hardware guarantee.

## Pause Fade

Locked pause fade:

```text
80ms
3840 frames @ 48kHz
```

FIX3 corrected a real final-frame off-by-one.

Final contract:

```text
fade frame 0       gain 1.0
...
fade frame 3839    gain > 0
fade frame 3840    silence, voice timeline frozen
```

Test:

```text
pause_fade_reaches_zero_in_exact_3840_frames_and_freezes_voice ... ok
```

## Physical Windows Device Smoke

Command:

```text
cargo run -p m32-audio --example windows_audio_smoke
```

Observed:

```text
device: FxSound Speakers(FxSound Audio Enhancer)
sample rate: 48000 Hz
channels: 2
callback buffer: device default (2880-frame request unsupported/unknown)
Playing a 440Hz M32 test tone for 1 second...
M32 audio smoke finished.
exit code 0
```

The user explicitly confirmed that the 440Hz test tone was audibly heard.

Machine-readable close marker:

```text
MANUAL_AUDIBLE_SMOKE: PASS
```

## Automated Bundle B Gate

```text
M32 0.0.5 Audio Bundle B functional verification passed.
exit code 0
```

Final relevant baselines:

```text
m32-audio       15/15
m32-wie-adapter 68/68
git diff --check exit 0
```

## Result

```text
0.0.5-T009 = DONE
```

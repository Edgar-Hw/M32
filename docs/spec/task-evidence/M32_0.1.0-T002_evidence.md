# M32 0.1.0-T002 Evidence — Explicit Local JAD+JAR Playable Launch

Status before human verification: IMPLEMENTED / NOT YET ACCEPTED

## Scope

The desktop product layer accepts exactly:

```text
--jad <local file.jad>
--jar <local file.jar>
```

Both paths are explicit and local. The launcher validates extensions, reads both files directly, reports
missing/unreadable input errors, and calls the already-proven `create_j2me_jad_jar_session`.

There is no synthetic fallback and no commercial/copyrighted game bundled.

## Automated proof

M32-owned deterministic WIE fixtures are copied to a temporary local directory and launched through
the same product-layer path.

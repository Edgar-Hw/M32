# M32 Task Evidence — 0.0.5-T002

- Task: `0.0.5-T002`
- Area: PCM i16 Mono/Stereo Normalization
- Version: `0.0.5 Audio`
- Bundle: `Bundle A`
- Rust: `1.98.0`
- Target: `x86_64-pc-windows-msvc`
- WIE revision: `f0513eb758c02736981f545ad030eed937d55f3e`
- Status: DONE


## Contract

```text
mono i16   -> f32 stereo duplicated L/R
stereo i16 -> f32 stereo preserving L/R order
f32        = i16 / 32768.0
```

Explicit failures:

```text
unsupported channel count
malformed interleaved stereo sample count
```

Tests:

```text
mono_i16_is_normalized_and_duplicated_to_stereo ... ok
stereo_i16_preserves_channel_order_and_rejects_malformed_input ... ok
```

FIX1 replaced fixed-size `chunks_exact(2)` with Rust 1.98 Clippy-preferred `as_chunks::<2>()`.
Audio math and behavior were unchanged.

```text
0.0.5-T002 = DONE
```

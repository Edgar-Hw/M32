# M32 Task Evidence — 0.0.1-T010

- Task: `0.0.1-T010`
- Area: crash handling
- Status: DONE
- Rust: `1.98.0`
- Target: `x86_64-pc-windows-msvc`

## 적용 내용

- M32 panic hook 추가
- Crash Report schema v1 추가
- BuildInfo 기반 crash provenance 출력
- panic message 단일-line 정규화
- panic message 최대 2048 characters 제한
- source path absolute-path 축소 정책 적용
- structured `m32::crash` panic event 추가
- Debug-only `M32_CRASH_TEST=panic` smoke-test trigger 추가
- backtrace 기본 미수집 정책 확정

## Desktop Unit Tests

```text
running 7 tests
...
test result: ok. 7 passed; 0 failed
```

Crash 4 tests + logging 3 tests PASS.

## Normal Run

검증 결과:

```text
M32 application started -> True
M32 Crash Report absent -> True
normal exit == 0 -> True
```

PASS.

## Intentional Panic Smoke Test

`M32_CRASH_TEST=panic` 실행 결과:

```text
crash exit == 101 -> True
M32 panic captured -> True
M32 Crash Report -> True
schema_version=1 -> True
product_spec_version=1.0.0 -> True
spec_bundle_version=1.0.1 -> True
git_commit -> True
WIE baseline commit -> True
rustc 1.98.0 -> True
target=x86_64-pc-windows-msvc -> True
build_profile=debug -> True
panic message -> True
source file/line/column -> True
```

Repository absolute path 검색 결과 없음.

```text
repo absolute path absent -> True
stack backtrace absent -> True
```

PASS.

PowerShell은 native stderr를 `NativeCommandError` 형태로 표시했으나, 이는 의도적
panic process의 stderr capture 방식 때문이며 실제 process exit code `101` 및
Crash Report 출력이 모두 확인되었으므로 acceptance 실패가 아님.

## Quality Gates

```text
cargo fmt --all -- --check
exit 0

cargo clippy --workspace --all-targets -- -D warnings
exit 0

cargo test --workspace
exit 0

cargo check --workspace
exit 0

git diff --check
exit 0
```

PASS.

전체 실제 unit tests:

- `m32-desktop`: 7 passed
- `m32-domain`: 3 passed
- `m32-test-fixtures`: 2 passed

최소 12 tests passed.

Windows working tree LF→CRLF 예정 경고가 일부 있었으나 `git diff --check`
exit code는 `0`.

## 판정

`0.0.1-T010 = DONE`

M32 panic을 구조화된 crash event와 bounded stderr crash report로 포착하는
Foundation crash-handling 기반이 검증되었다.

## Next Exact Task

`0.0.1-T011`

# M32 0.1.0-T005 Evidence — Realtime Audio Output Wiring

Status before verification: IMPLEMENTED / NOT YET ACCEPTED

## Scope

The product composition root now opens one Windows `CpalOutputStream` for an active playable runtime and keeps it alive for the full runtime lifetime. WIE guest audio is delegated into the existing `RealtimeGuestAudioHost`; the audio engine is not reimplemented.

Locked audio contracts remain owned by `m32-audio`:

```text
48000Hz
f32 stereo
60ms / 2880-frame target
80ms / 3840-frame pause fade
try_lock callback -> silence on transient contention
```

Automated Bundle B verification checks the production stream ownership path and the existing canonical audio contract. Physical audible proof remains the dedicated T010 Windows manual First Playable gate.

# M32 Task Evidence — 0.0.1-T011

- Task: `0.0.1-T011`
- Area: runtime paths
- Status: DONE
- Rust: `1.98.0`
- Target: `x86_64-pc-windows-msvc`

## 적용 내용

- Windows v1.0 runtime root를 `%LOCALAPPDATA%\M32`로 고정
- `config.json` 안정 경로 계약 정의
- `cache`, `logs`, `crashes` 디렉터리 계약 정의
- startup runtime path discovery/creation 구현
- current directory fallback 금지
- path discovery/directory creation 실패 시 exit code `2`
- DEBUG `runtime_paths_ready` event 추가
- INFO/DEBUG 로그에 absolute LOCALAPPDATA path를 기본 노출하지 않는 정책 적용

## Desktop Unit Tests

```text
running 9 tests
...
test paths::tests::directory_names_match_locked_contract ... ok
test paths::tests::windows_local_app_data_layout_is_stable ... ok

test result: ok. 9 passed; 0 failed
```

PASS.

## Runtime Directory Acceptance

정상 실행:

```text
normal exit == 0 -> True
```

실제 생성 확인:

```text
%LOCALAPPDATA%\M32          -> True
%LOCALAPPDATA%\M32\cache   -> True
%LOCALAPPDATA%\M32\logs    -> True
%LOCALAPPDATA%\M32\crashes -> True
```

PASS.

## Logging Privacy

DEBUG 실행에서:

```text
runtime_paths_ready -> True
LOCALAPPDATA absolute path absent -> True
```

PASS.

## Missing LOCALAPPDATA Failure Path

현재 PowerShell process에서 `LOCALAPPDATA`를 일시 제거한 acceptance test:

```text
exit code == 2 -> True
path_discovery_failed -> True
LOCALAPPDATA is unavailable -> True
```

환경 변수는 테스트 직후 원래 값으로 복원됨.

PASS.

## Quality Gates

```text
cargo fmt --all -- --check
exit 0

cargo clippy --workspace --all-targets -- -D warnings
exit 0

cargo test --workspace
exit 0

cargo check --workspace
exit 0

git diff --check
exit 0
```

PASS.

전체 실제 unit tests:

- `m32-desktop`: 9 passed
- `m32-domain`: 3 passed
- `m32-test-fixtures`: 2 passed

최소 14 tests passed.

Windows working tree의 LF→CRLF 예정 경고가 있었으나
`git diff --check` exit code는 `0`.

## 판정

`0.0.1-T011 = DONE`

Windows v1.0의 M32 persistent runtime root와 stable config/cache/log/crash path contract가
코드와 실제 실행에서 검증되었다.

## Next Exact Task

`0.0.1-T012`

# M32 Task Evidence — 0.0.2-T001

- Task: `0.0.2-T001`
- Version: `0.0.2 Core Adapter`
- Area: WIE upstream dependency pin
- Status: DONE
- Rust: `1.98.0`
- Target: `x86_64-pc-windows-msvc`

## Locked Upstream

WIE:

```text
repository=https://github.com/dlunch/wie.git
revision=f0513eb758c02736981f545ad030eed937d55f3e
crate=wie_backend
```

Resolved Cargo source:

```text
git+https://github.com/dlunch/wie.git?rev=f0513eb758c02736981f545ad030eed937d55f3e#f0513eb758c02736981f545ad030eed937d55f3e
```

## FIX1 — SMAF transitive Git pin

Initial WIE resolution failed because the pinned WIE manifest referenced `smaf` / `smaf_player`
through a mutable Git dependency while the current SMAF HEAD no longer exposed the package layout
required by the WIE revision.

The SMAF revision recorded by the pinned WIE lockfile was recovered and locked:

```text
repository=https://github.com/dlunch/smaf.git
revision=8009d78512fd121609a841f31aa527bf2a4af456
```

Minimal required production packages were vendored:

```text
third_party/smaf/smaf
third_party/smaf/smaf_player
```

Cargo root path patch verified both packages resolve from those vendored directories.

## FIX2 — tracing compatibility

After FIX1, Cargo reached WIE's tracing constraints.

Conflict:

```text
M32 tracing 0.1.44 -> tracing-attributes 0.1.31
pinned WIE wie_util -> tracing-attributes <0.1.29
```

The pinned WIE lock baseline uses:

```text
tracing=0.1.41
tracing-attributes=0.1.28
tracing-core=0.1.36
```

M32 direct tracing was therefore compatibility-pinned:

```text
tracing 0.1.44 -> 0.1.41
```

`tracing-subscriber 0.3.23` remained unchanged.

Final resolved versions:

```text
tracing v0.1.41
tracing-attributes v0.1.28
tracing-core v0.1.36
```

## Adapter Build

```text
cargo check -p m32-wie-adapter
exit 0
```

The build compiled:

- vendored `smaf`
- vendored `smaf_player`
- `wie_util` at the pinned WIE revision
- `wie_backend` at the pinned WIE revision
- `m32-wie-adapter`

PASS.

## Upstream Verification

Canonical verifier output confirmed:

```text
[PASS] smaf path patch
[PASS] smaf_player path patch
[PASS] WIE repository
[PASS] WIE revision
[PASS] full WIE Cargo source revision
[PASS] SMAF repository
[PASS] SMAF revision
[PASS] tracing 0.1.41
[PASS] tracing-attributes 0.1.28
[PASS] m32-wie-adapter directly depends on wie_backend
WIE upstream verification passed.
```

Verifier exit code:

```text
0
```

PASS.

## Adapter Unit Tests

```text
running 2 tests
test tests::pinned_wie_backend_options_api_is_available ... ok
test tests::upstream_revision_matches_locked_baseline ... ok

test result: ok. 2 passed; 0 failed
```

PASS.

## Workspace Tests

Actual tests maintained:

- `m32-desktop`: 9 passed
- `m32-domain`: 3 passed
- `m32-test-fixtures`: 2 passed
- `m32-wie-adapter`: 2 passed

Minimum total:

```text
16 tests passed
```

## Quality Gates

```text
cargo fmt --all -- --check
exit 0

cargo clippy --workspace --all-targets -- -D warnings
exit 0

cargo test --workspace
exit 0

cargo check --workspace --all-targets
exit 0

git diff --check
exit 0
```

PASS.

Windows LF→CRLF notices from `git diff --check` were warnings only; exit code was `0`.

## Lockfile Result

The root Cargo graph successfully resolved the pinned WIE backend and its transitive requirements.
The successful resolution added the WIE dependency set and changed the M32 tracing baseline to the
WIE-compatible exact version.

The root `Cargo.lock` is part of the T001 commit and must not be casually updated.

## 판정

`0.0.2-T001 = DONE`

M32 now has a reproducibly pinned, compiling `m32-wie-adapter -> wie_backend` dependency boundary
without yet leaking WIE types into M32 application-facing APIs.

## Next Exact Task

`0.0.2-T002`

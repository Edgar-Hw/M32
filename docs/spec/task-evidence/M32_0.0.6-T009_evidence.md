# M32 Task Evidence — 0.0.6-T009

- Task: `0.0.6-T009`
- Area: Persistent Filesystem WIE Integration / Restart Proof
- Version: `0.0.6 Storage / Persistence`
- Bundle: `Bundle B`
- Rust: `1.98.0`
- Target: `x86_64-pc-windows-msvc`
- Status: DONE


## Integration Path

First platform:

```text
WIE Filesystem adapter
-> M32 GuestFilesystemHost
-> DiskGuestFilesystem
-> persistent host file
```

Locked test identity:

```text
AID  = M32 Persistent Filesystem
path = save/state.bin
data = M32-FS-PERSIST
```

After the first WIE platform and storage object are destroyed, a new storage object and WIE platform
are created against the same M32 storage root.

The rebuilt platform verifies:

```text
exists == true
size == exact payload size
read == exact persisted bytes
```

Test:

```text
persistent_wie_filesystem_survives_platform_rebuild ... ok
```

Verifier:

```text
[PASS] T009 WIE filesystem adapter writes persistent M32 disk host
[PASS] T009 rebuilt WIE platform reads exact persistent guest file bytes
```

## Scope Boundary

This task proves the WIE filesystem adapter and persistent M32 disk host across restart.

It does not claim a complete Java JSR-75 `FileConnection` implementation.

## Functional Bundle Result

```text
M32 0.0.6 Storage Bundle B functional verification passed.
exit code 0
```

Regression chain from the same verifier also passed:

```text
Storage Bundle A
Audio version close
Input
First Frame
Core Adapter
```

```text
0.0.6-T009 = DONE
```

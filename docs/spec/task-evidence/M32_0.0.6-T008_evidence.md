# M32 Task Evidence — 0.0.6-T008

- Task: `0.0.6-T008`
- Area: Real Guest Save -> Session Rebuild -> Load Proof
- Version: `0.0.6 Storage / Persistence`
- Bundle: `Bundle B`
- Rust: `1.98.0`
- Target: `x86_64-pc-windows-msvc`
- Status: DONE


## End-to-end Persistence Proof

First run:

```text
PersistentGuestStorage #1
-> WIE Platform #1
-> new J2ME emulator session #1
-> RmsPersistenceMidlet
-> RecordStore.getNumRecords() == 0
-> addRecord("M32-RMS1")
-> record id 1
-> guest save sentinel
```

Then both the emulator session and storage object are destroyed.

Second run against the same storage root:

```text
PersistentGuestStorage #2
-> WIE Platform #2
-> new J2ME emulator session #2
-> same RmsPersistenceMidlet
-> RecordStore.getNumRecords() > 0
-> getRecord(1)
-> exact 8-byte comparison
-> guest load-success sentinel
```

This proves persistence across both:

```text
emulator session reconstruction
storage host reconstruction
```

rather than only reusing an in-memory Rust object.

Test:

```text
real_j2me_rms_survives_session_and_storage_rebuild ... ok
```

Verifier:

```text
[PASS] T008 real Java RecordStore save reaches persistent M32 SQLite host
[PASS] T008 emulator + storage reconstruction reloads exact saved guest bytes
[PASS] T008 stable MIDlet-Name application namespace and exact 8-byte usage
```

Final `m32-wie-adapter` Bundle B baseline:

```text
72 passed
0 failed
```

```text
0.0.6-T008 = DONE
```

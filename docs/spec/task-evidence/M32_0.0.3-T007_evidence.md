# M32 Task Evidence — 0.0.3-T007

- Task: `0.0.3-T007`
- Version: `0.0.3 First Frame`
- Area: RGBA8 Frame Dimension/Content Lock
- Status: DONE
- Rust: `1.98.0`
- Target: `x86_64-pc-windows-msvc`
- WIE revision: `f0513eb758c02736981f545ad030eed937d55f3e`
- RustJava base revision: `ba5797b8eb4cf376fdd63129903d319d1d7acf98`

## Goal

Convert the real guest-generated first-frame smoke from T006 into an exact framebuffer contract.

The frame is obtained through the real runtime path:

```text
PaintMidlet
-> Display.setCurrent
-> Event::Redraw
-> MIDP EventQueue
-> PaintCanvas.paint
-> WIE screen.paint
-> WieScreenAdapter
-> FirstFrameCaptureDisplayHost
```

No expected frame is injected into the display host.

## Locked Dimensions

```text
width  = 176
height = 220
```

Pixel format:

```text
RGBA8
4 bytes per pixel
```

Exact framebuffer byte length:

```text
176 * 220 * 4 = 154880 bytes
```

Exact pixel count:

```text
176 * 220 = 38720 pixels
```

## Locked Paint Pattern

Top-left marker:

```text
x = 0..15
y = 0..15

RGBA = [0xD1, 0x4A, 0x36, 0xFF]
#D14A36FF
```

Exact RED pixel count:

```text
16 * 16 = 256
```

All remaining pixels:

```text
RGBA = [0x0E, 0x11, 0x14, 0xFF]
#0E1114FF
```

Exact BG0 pixel count:

```text
38720 - 256 = 38464
```

## Full-frame Validation

Every coordinate in the captured framebuffer is validated.

For every `(x,y)`:

```text
x < 16 && y < 16
-> M32 RED

otherwise
-> M32 BG0
```

Any mismatch reports the exact coordinate.

Explicit boundary samples are also locked:

```text
(0,0)       RED
(15,0)      RED
(0,15)      RED
(15,15)     RED

(16,0)      BG0
(0,16)      BG0

(175,219)   BG0
```

All alpha bytes are therefore also locked to:

```text
255
```

## Targeted Test

```text
first_frame_paint_fixture_locks_exact_rgba8_dimensions_and_content ... ok
```

Result:

```text
1 passed
0 failed
exit 0
```

## Adapter Tests

```text
running 56 tests

test result: ok. 56 passed; 0 failed
```

PASS.

## Emulator API Tests

```text
running 29 tests

test result: ok. 29 passed; 0 failed
```

PASS.

## T007 Verifier

```text
[PASS] exact 176x220 first-frame dimensions
[PASS] exact 154880-byte RGBA8 framebuffer
[PASS] exact 38720-pixel frame
[PASS] exact 16x16 M32 RED marker region
[PASS] exact 256 RED pixel count
[PASS] exact M32 BG0 background region
[PASS] exact 38464 BG0 pixel count
[PASS] all alpha bytes locked to 255 through exact pixel values
[PASS] every framebuffer coordinate validated
[PASS] first captured frame remains the locked frame
[PASS] previous real first-frame verification chain
First-frame RGBA8 content verification passed.
```

Exit code:

```text
0
```

PASS.

The complete T006/T005/T004/T003/T002/T001/Core Adapter chain reran successfully.

## Workspace Tests

Actual M32 unit tests:

- `m32-desktop`: 9
- `m32-domain`: 3
- `m32-emulator-api`: 29
- `m32-test-fixtures`: 2
- `m32-wie-adapter`: 56

Minimum total:

```text
99 tests passed
```

## Quality Gates

```text
cargo fmt --all
exit 0

cargo fmt --all -- --check
exit 0

cargo check -p m32-wie-adapter
exit 0

cargo clippy --workspace --all-targets -- -D warnings
exit 0

cargo test --workspace
exit 0

cargo check --workspace --all-targets
exit 0

git diff --check
exit 0
```

PASS.

LF->CRLF warning remained non-blocking.

## Repository Scope

Final status:

```text
 M crates/m32-wie-adapter/src/lib.rs
?? docs/test/WIE_FIRST_FRAME_RGBA8_CONTENT_LOCK.md
?? scripts/verify-wie-first-frame-rgba8-content.ps1
```

No Cargo.lock change.

No WIE/RustJava change.

No guest fixture change.

## 판정

```text
0.0.3-T007 = DONE
```

## Next Task

```text
0.0.3-T008
First-frame Timeout/Failure Boundary
```

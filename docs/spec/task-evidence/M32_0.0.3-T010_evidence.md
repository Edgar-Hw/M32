# M32 Task Evidence — 0.0.3-T010

- Task: `0.0.3-T010`
- Version: `0.0.3 First Frame`
- Area: First Frame Version Close / Gates
- Status: DONE
- Rust: `1.98.0`
- Target: `x86_64-pc-windows-msvc`
- WIE revision: `f0513eb758c02736981f545ad030eed937d55f3e`
- RustJava base revision: `ba5797b8eb4cf376fdd63129903d319d1d7acf98`

## Goal

Close the complete `0.0.3 First Frame` version after T001 through T009 have been implemented,
validated, evidenced, and committed.

T010 introduces no new emulator behavior.

It is the version-close gate.

## T010 Initial Failure

The first version-close verifier run failed because the verifier searched for evidence names such as:

```text
0.0.3-T001.md
```

The repository already contained the actual established evidence naming convention:

```text
M32_0.0.3-T001_evidence.md
...
M32_0.0.3-T009_evidence.md
```

The evidence was present; the verifier's filename assumption was incorrect.

## FIX1 — Evidence Naming Convention

The T010 verifier was corrected to use:

```text
M32_<TaskId>_evidence.md
```

For example:

```text
TaskId       = 0.0.3-T001
EvidenceFile = M32_0.0.3-T001_evidence.md
```

No evidence files were renamed or duplicated.

This preserves the repository's existing task-evidence convention.

## Evidence Set Gate

After FIX1, the close verifier validates all nine previous First Frame task evidence files:

```text
M32_0.0.3-T001_evidence.md
M32_0.0.3-T002_evidence.md
M32_0.0.3-T003_evidence.md
M32_0.0.3-T004_evidence.md
M32_0.0.3-T005_evidence.md
M32_0.0.3-T006_evidence.md
M32_0.0.3-T007_evidence.md
M32_0.0.3-T008_evidence.md
M32_0.0.3-T009_evidence.md
```

PASS.

## Canonical Integration Gate

T010 invokes:

```text
scripts/verify-wie-first-frame-integration.ps1
```

This revalidates the complete T001-T009 First Frame chain.

The canonical First Frame outcomes remain:

```text
SUCCESS
-> real guest-generated exact RGBA8 first frame

TIMEOUT
-> healthy Running guest
-> no frame within bounded wait

FAULT
-> backend/session failure before first frame
-> failure is not mislabeled as timeout
```

PASS.

## Closed First Frame Contract

`0.0.3` now establishes:

```text
explicit JAD+JAR launch path
deterministic synthetic MIDlet fixtures
real RustJava guest application-class loading
MIDlet.startApp positive boot
Ready -> Running lifecycle
FirstFrameCaptureDisplayHost semantics
first-present immutability
Display.setCurrent repaint request
deterministic executor-time advancement
MIDP EventQueue redraw dispatch
real Canvas.paint(Graphics)
Graphics.setColor / fillRect execution
WIE screen.paint integration
WieScreenAdapter RGBA8 conversion
actual guest-generated framebuffer capture
exact 176x220 dimensions
exact 154880 framebuffer bytes
exact 38720 pixels
exact 16x16 M32 RED region
exact 256 RED pixels
exact 38464 M32 BG0 pixels
all exact RGBA values including alpha 255
healthy no-frame timeout
backend fault precedence over timeout
canonical SUCCESS / TIMEOUT / FAULT verifier
actual captured BMP developer preview
```

## Regression Baseline

The First Frame close baseline is:

```text
m32-desktop          9
m32-domain           3
m32-emulator-api    29
m32-test-fixtures    2
m32-wie-adapter     58
----------------------
minimum total       101
```

## Version-close Quality Gates

The T010 verifier runs and requires:

```text
cargo fmt --all -- --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
cargo check --workspace --all-targets
git diff --check
```

It also requires:

```text
Cargo.lock has no uncommitted change
third_party/rustjava/jvm/src/jvm.rs has no uncommitted change
```

After the evidence-naming FIX1, the user reported the complete version-close verifier finished with:

```text
exit code 0
```

PASS.

## Dependency / Vendor Boundary

No new T010 dependency.

No Cargo.lock change.

No additional WIE source/revision change.

No additional RustJava source change.

The T003 RustJava compatibility patch remains committed history and is covered by the existing
verification chain.

## Out of Scope

`0.0.3 First Frame` does not claim:

```text
interactive keypad input
desktop runtime controller routing
key repeat
multi-key limits
audio completion
persistent save support
broad real-game compatibility
```

Those belong to subsequent versions.

The immediate next version is:

```text
0.0.4 Input
```

## Final 판정

```text
0.0.3-T010 = DONE
0.0.3 First Frame = 10 / 10 DONE
```

## Overall Progress After Close

```text
0.0.1 Foundation    12 / 12
0.0.2 Core Adapter  12 / 12
0.0.3 First Frame   10 / 10

Total DONE          34 / 253
Remaining           219 / 253
```

## Next Version

```text
0.0.4 Input
10 tasks
```

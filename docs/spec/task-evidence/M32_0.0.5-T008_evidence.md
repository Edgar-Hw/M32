# M32 Task Evidence — 0.0.5-T008

- Task: `0.0.5-T008`
- Version: `0.0.5 Audio`
- Area: Sequence / Voice / Repeat / Baseline MIDI Renderer
- Status: DONE

## Runtime

`RealtimeGuestAudioHost` accepts the stable `GuestAudioHost` command contract and owns deterministic
audio voices keyed by the exact `u32` guest audio handle.

Wave events are canonicalized once and scheduled by:

```text
event milliseconds -> exact 48kHz frame position
```

Supported voice lifecycle:

```text
Play
Stop by handle
repeat
sequence end
deterministic timeline
```

Tests:

```text
realtime_host_schedules_wave_at_exact_millisecond_frame ... ok
stop_by_handle_removes_active_voice_before_next_render ... ok
repeat_restarts_prepared_wave_deterministically ... ok
```

## Baseline MIDI

Initial playable renderer supports:

```text
Note On
Note Off
Note On velocity 0 -> Note Off
CC120 All Sound Off
CC123 All Notes Off
```

Pitch is rendered deterministically from MIDI note number using a sine oscillator.

Test:

```text
baseline_midi_note_on_off_renders_signal_then_stops ... ok
```

This task does NOT claim full General MIDI/SMAF timbre fidelity. Program changes, SoundFont-quality
instruments, SysEx timbre, and vendor-specific SMAF synthesis remain compatibility-expansion scope.

## Result

```text
0.0.5-T008 = DONE
```

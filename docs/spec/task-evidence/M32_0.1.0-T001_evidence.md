# M32 0.1.0-T001 Evidence — Native Desktop Shell

Status before verification: IMPLEMENTED / NOT YET ACCEPTED

## Scope
- Adds native `winit 0.30.13` ApplicationHandler event loop.
- Creates the M32 window at 1440x900 logical pixels.
- Enforces the locked 1180x700 minimum size.
- Keeps existing logging, crash and runtime-path startup before opening UI.
- Does not add direct WIE, audio or storage dependencies to `m32-desktop`.

## Files
- `apps/m32-desktop/src/desktop.rs`
- `apps/m32-desktop/src/main.rs`
- `apps/m32-desktop/Cargo.toml`
- root `Cargo.toml`

## Verification
`powershell -ExecutionPolicy Bypass -File scripts\verify-first-playable-bundle-a.ps1`

Acceptance requires the verifier and previous `0.0.6` version-close chain to pass.

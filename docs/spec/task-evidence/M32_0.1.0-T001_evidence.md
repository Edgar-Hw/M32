# M32 0.1.0-T001 Evidence — Desktop Runtime Composition Root

Status before human verification: IMPLEMENTED / NOT YET ACCEPTED

## Scope

`apps/m32-desktop/src/composition.rs` now owns the product-layer concrete runtime assembly:

```text
LiveDisplayHost
SystemClockHost
DesktopOutputHost
DesktopExitHost
DesktopVibrationHost
RealtimeGuestAudioHost
PersistentGuestStorage database/filesystem hosts
WiePlatformHosts
JAD+JAR WIE session
```

SQLite details remain in `m32-storage`. WIE-specific translation remains in `m32-wie-adapter`.

## Verification

```powershell
cargo test -p m32-desktop composition::tests
powershell -ExecutionPolicy Bypass -File scripts\verify-first-playable-bundle-a.ps1
```

Acceptance requires the full Bundle A verifier and previous 0.0.6 version-close chain to pass.

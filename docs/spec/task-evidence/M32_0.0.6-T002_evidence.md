# M32 Task Evidence — 0.0.6-T002

- Task: `0.0.6-T002`
- Area: Persistent RMS Database Repository
- Version: `0.0.6 Storage / Persistence`
- Bundle: `Bundle A`
- Rust: `1.98.0`
- Target: `x86_64-pc-windows-msvc`
- Status: DONE


## Contract

Persistent guest database state is stored in SQLite tables:

```text
guest_databases
guest_records
```

Logical isolation key:

```text
(app_id, database_name)
```

Repository contract:

```text
open
exists
delete
usage
```

Record contract:

```text
next_id
add
get
set
delete
record_ids
```

Record IDs:

```text
start at 1
monotonically advance
deleted IDs are not reused
sorted ascending on record_ids()
```

Tests:

```text
repository_open_exists_delete_are_scoped_by_app_and_name ... ok
record_ids_start_at_one_and_do_not_reuse_deleted_ids ... ok
record_get_set_delete_and_sorted_ids_round_trip ... ok
```

Verifier:

```text
[PASS] T002 persistent RMS repository and record lifecycle
[PASS] T002 monotonic non-reused record IDs
```

```text
0.0.6-T002 = DONE
```

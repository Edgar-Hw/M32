# M32 Task Evidence — 0.0.2-T007

- Task: `0.0.2-T007`
- Version: `0.0.2 Core Adapter`
- Area: Guest Filesystem Host Contract / WIE Filesystem Bridge
- Status: DONE
- Rust: `1.98.0`
- Target: `x86_64-pc-windows-msvc`
- Emulator API schema: `1`

## 적용 내용

- M32-owned `HostFuture<'a, T>` 추가
- object-safe asynchronous `GuestFilesystemHost` 추가
- `GuestFilesystemError` / `GuestFilesystemErrorCode` 추가
- pinned WIE Filesystem의 AID-scoped async surface를 `WieFilesystemAdapter`로 연결
- AID/path/offset/count/write/truncate 값 보존
- WIE fallback error mapping 추가
- invalid read-count / partial-write count 보호
- storage warning에서 guest path, AID, host path, free-form error message 비노출
- exact `async-trait = 0.1.92` workspace dependency 고정
- `m32-emulator-api` WIE dependency-free boundary 유지

## Functional Validation

Source application:

```text
verify-wie-filesystem-bridge.ps1 = present
GuestFilesystemHost = present
WieFilesystemAdapter = present
```

### Emulator API

```text
running 18 tests
...
test tests::guest_filesystem_error_keeps_stable_code_and_message ... ok
test tests::guest_filesystem_contract_preserves_aid_path_offsets_and_bytes ... ok

test result: ok. 18 passed; 0 failed
```

PASS.

### WIE Adapter

```text
running 21 tests
...
test tests::wie_filesystem_adapter_implements_pinned_filesystem_contract ... ok
test tests::wie_filesystem_exists_and_size_preserve_aid_and_path ... ok
test tests::wie_filesystem_read_preserves_offset_count_and_buffer_result ... ok
test tests::wie_filesystem_write_and_truncate_preserve_request_values ... ok
test tests::wie_filesystem_host_errors_map_to_wie_fallback_values ... ok
test tests::wie_filesystem_rejects_invalid_host_read_and_write_counts ... ok

test result: ok. 21 passed; 0 failed
```

PASS.

## Filesystem Verifier

```text
[PASS] object-safe M32 GuestFilesystemHost contract
[PASS] AID/path-preserving WIE filesystem bridge
[PASS] read offset/count/buffer mapping
[PASS] write/truncate mapping
[PASS] host-error fallback mapping
[PASS] invalid read/write count protection
[PASS] direct async-trait adapter dependency
[PASS] existing emulator API dependency boundary
WIE filesystem bridge verification passed.
```

Exit code:

```text
0
```

PASS.

## Dependency Boundary

Adapter depth-1 graph:

```text
m32-wie-adapter
├── async-trait v0.1.92
├── m32-emulator-api
├── tracing v0.1.41
├── wie_backend @ f0513eb758c02736981f545ad030eed937d55f3e
└── wie_util @ f0513eb758c02736981f545ad030eed937d55f3e
```

`m32-emulator-api` remains WIE-independent.

PASS.

## FIX1 — Clippy test helper type complexity

Initial full T007 Clippy gate found one test-only issue:

```text
clippy::type-complexity
Mutex<Option<(String, String, usize, Vec<u8>)>>
```

Production filesystem bridge, verifier, and unit tests were already passing.

FIX1 replaced tuple-based recording state with explicit test helper structs:

```text
RecordedWrite
RecordedTruncate
```

No production behavior, public API, dependency, or WIE revision changed.

## Final Quality Gates

After FIX1:

```text
cargo fmt --all -- --check
exit 0

cargo test -p m32-wie-adapter
21 passed
exit 0

cargo clippy --workspace --all-targets -- -D warnings
exit 0

cargo test --workspace
exit 0

cargo check --workspace --all-targets
exit 0

git diff --check
exit 0
```

LF→CRLF messages from `git diff --check` were warnings only.

Workspace actual tests:

- `m32-desktop`: 9
- `m32-domain`: 3
- `m32-emulator-api`: 18
- `m32-test-fixtures`: 2
- `m32-wie-adapter`: 21

Minimum total:

```text
53 tests passed
```

## 판정

`0.0.2-T007 = DONE`

M32 now owns an object-safe asynchronous guest-filesystem boundary, and the pinned WIE Filesystem is
adapted without interpreting guest paths as Windows host paths.

## Next Exact Task

`0.0.2-T008`
